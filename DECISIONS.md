# DECISIONS.md — Calendar-Year Plan Loss Ratio Workbook

Every judgment call made while building this tool, and why. Section references (§) point to the
build brief (`plan_lr_workbook_claude_code_prompt.md`).

---

## 1. Method restatement (in my own words)

A rate level indication produces a projected loss ratio `LR_proj` for the policy year written
around the indication's assumed effective date, 7/1/(P−1). Because an annual policy year effective
7/1/(P−1) has its average loss date at 7/1/P — the midpoint of plan year P — the indication's loss
projection is already trended to the plan year's loss center of gravity, so **no additional loss
trend is applied for the plan-year result**.

The premium side is where the bases differ. The indication's loss ratio assumes premium at the
indication's cumulative rate level `CRL_ind` (the product of all rate changes the indication
considered), **fully earned**. The calendar year P will not actually earn premium at that level:
old rates still earn in during the early months (carryover), and planned changes earn in only
partially. The engine therefore builds, month by month:

1. A **written rate index** `W_k` for each monthly writing cohort `k` — the cumulative product of
   all rate changes effective on or before that cohort's writing date (base 1.000 before the first
   logged change). Planned changes contribute `filed % × achievement %`.
2. An **earning matrix** `e(k, m)` — how much of cohort `k`'s premium earns in calendar month `m`,
   using the standard mid-month convention for a `T`-month policy term: `1/(2T)` in the writing
   month, `1/T` in each of the next `T−1` months, `1/(2T)` in month `k+T`.
3. The **calendar-year earned rate index** — the exposure-weighted aggregate
   `E_CY(Y) = Σ_m Σ_k w_k·e(k,m)·W_k / Σ_m Σ_k w_k·e(k,m)`, with `w_k` the written weight
   (uniform by default, optional monthly seasonality).

The **rate adjustment factor** is `A_rate(P) = CRL_ind / E_CY(P)`: it restates the projected loss
ratio from "indication rate level, fully earned" to "what CY P actually earns."

Schedule mod drift is the same economics through a different lever: the average schedule mod is a
multiplicative premium modifier fixed at issuance, so drift in the average written mod earns in on
written business exactly like rate. The written mod path `M_w(k)` is piecewise-linear through the
anchor points supplied by the user; the earned mod `M̄_E(Y)` is the exposure-weighted **average**
(not product — mods are levels) through the same earning matrix, and
`A_mod(P) = M_ind / M̄_E(P)`.

The bridge is then:

```
LR_current = LR_input × (1 + s)   if input basis is 'proposed', else LR_input
CY_LR(P)   = LR_current × A_rate(P) × A_mod(P) × A_other
CY_LR(P+1) = LR_current × (1 + net_trend) × A_rate(P+1) × A_mod(P+1) × A_other   [indicative]
```

Directionality intuition (encoded as property tests): a planned increase the indication did not
consider adds earned premium the indication's LR never contemplated ⇒ `A_rate < 1` (CY LR below
projected). A considered change that CY P has not fully earned means CY premium sits *below* the
indication's fully-earned level ⇒ pushes `A_rate > 1` (carryover shortfall). Mods rising above
`M_ind` ⇒ more premium than the indication assumed ⇒ `A_mod < 1`.

Hand verification of the §9 worked example (done before any code):
`CRL_ind = 1.04 × 1.06 = 1.1024`. CY-2027 parallelogram areas for annual policies, uniform
writings: 0.125 @ 1.0400 (pre-7/1/2026 rates), 0.59375 @ 1.1024, 0.28125 @ 1.157520
(areas sum to 1.000) → `E_CY = 1.1101025` → `A_rate = 0.9930617`. Mod path 0.860 (close of
9/30/2026) → 0.890 (close of 12/31/2027) is 15 months at 0.002/month → written mod at 1/1/2027
= 0.866; by the linear-mod identity `M̄_E(2027) = 0.8660` → `A_mod = 0.850/0.866 = 0.9815242`.
`CY LR = 65.0% × 0.9930617 × 0.9815242 = 63.36%`. All stated values reproduced.

---

## 2. Timeline and convention decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | **Cohort window extended to 48 months** — Jan (P−2) through Dec (P+1) — rather than the brief's 36 months (§3.2.1). | §3.4.4 requires `E_CY(P−1)` for the year-over-year earned rate change, and CY (P−1) earns from cohorts written as early as Jan (P−2) for a 12-month term. The 36-month window cannot produce `E_CY(P−1)`. Earned months reported: Jan (P−1) – Dec (P+1), 36 months. This is the *only* deviation from a stated §3 parameter, made because the brief is internally inconsistent on this point; everything downstream (earned window, dashboards) matches the brief. |
| D2 | **Rate-change effective dates are start-of-day; mod as-of anchors are close-of-day.** | A change "effective 7/1" applies to business written on 7/1 (per the §3.2.2 pro-rata formula, a day-1 effective date gives the full month the new rate). A mod "as of 9/30/2026" is a measurement at the close of that day, i.e., the 10/1 boundary. This is also the only reading that reproduces the worked example exactly: 9/30/2026→12/31/2027 is then exactly 15 months, giving written mod 0.8660 at 1/1/2027 (start-of-day anchoring gives 0.86607 and misses the stated `A_mod = 0.98152`). In Excel, mod anchor serials are `date + 1`, labeled in-sheet. |
| D3 | **Continuous (closed-form) engine measures time in months** (each month = 1/12 year; intra-month dates pro-rata by day) rather than actual day counts. | Reproduces textbook parallelogram geometry and the brief's stated expected values exactly (7/1 = 0.500, 4/1 = 0.250 of a year). The monthly production engine uses real day counts only where the brief demands it (intra-month pro-rata splits, mod-path interpolation on date serials); differences are tiny and covered by the ±0.10-pt monthly-vs-continuous tolerance. |
| D4 | **Multiple rate changes in one cohort month for the same BU\|LOB**: the written weight is split at the *first* change date between the pre-month index and the full end-of-month index (all changes compounded). Exact when ≤ 1 change per month per combo — the realistic case; a `Checks` warning fires when two changes share a month. | The §3.2.2 pro-rata rule is stated for one change; a fully general intra-month daily average is not expressible in auditable classic-mode Excel. The oracle implements the same convention (plus an exact-daily cross-check used in tests), so Excel ties the oracle exactly. |
| D5 | **Written index / `CRL_ind` share base 1.000 "before all logged changes."** `W_k` is the product of *all* changes effective ≤ cohort date; `CRL_ind` the product over `considered = Y` rows. | `A_rate` is a ratio, so only relative consistency matters. A pre-window change flagged not-considered still (correctly) moves `W` but not `CRL_ind`. |
| D6 | **Mod path beyond the last anchor extends the last segment linearly; before the first anchor, extends the first segment** (the `M_prior` segment when given, else the `M_0→M_1` segment, per §3.3.1). Cohort mod is evaluated at the cohort's actual mid-month date. | Matches §3.3.1; documented on the Mod Engine and Methodology sheets. Cohort mid-months never actually exceed the 12/31/(P+1) anchor within the 48-month window. |
| D7 | **A planned row flagged `considered = Y`** is included in `CRL_ind` at its effective (achievement-adjusted) value, with a warning on `Checks`. | §3.2.7 says validate and warn, not exclude; including it keeps `CRL_ind` equal to "product of flagged rows" (the §9 check) under all inputs. |
| D8 | **Achievement % applies to `planned` rows only**; `taken` rows always use filed % (achievement column ignored, greyed by convention). | §3.1 definition. |

## 3. Architecture decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D9 | **Excel recalculation via Excel COM automation** (`tools/recalc.py`), since LibreOffice is not installed on the build machine but desktop Excel 16.0 is. The script also supports `soffice --headless` when present. | §9 allows "an equivalent recalc script"; native Excel is the authoritative calc engine, strictly better than a LibreOffice proxy. Classic-mode formulas are still used throughout so LibreOffice validation remains possible anywhere. |
| D10 | **`_calc` engine blocks use a closed-form year-bucket earning column** instead of repeating the 36-column monthly matrix per combo: for cohort `k` and year `Y`, `ec(k,Y) = C(mEnd−k) − C(mStart−1−k)` where `C(o) = MIN(1, MAX(0, (o+0.5)/T))` is the cumulative mid-month earning curve. Mathematically identical to summing `e(k,m)` over `m ∈ Y` (proved in `tests/test_engine.py::test_ec_closed_form_equals_matrix_sums`). | Keeps the hidden per-combo blocks compact (§5.8 "compact engine block") and under the 120-char formula target. The visible `Rate Engine` sheet still shows the full 48×36 matrix for auditability, and a `Checks` row ties the visible matrix aggregate to the compact form for the selected combo — a live cross-method validation. |
| D11 | **Formulas reference fixed named column ranges** (e.g., `rl_eff`, `lr_key`) that exactly cover the input tables, not structured table references (`tbl_RateLog[...]`). Tables are still real Excel Tables for UX; their sizes are fixed (40 rate-log rows, 18 LR rows, 6 seasonality rows) with blank-tolerant formulas. | Structured references recalculate inconsistently in LibreOffice and grow/shrink with table edits, silently breaking fixed engine ranges. Fixed named ranges are classic-mode-safe, auditable by name, and documented on `Methodology`. Spare blank rows are included; the row counts are stated on `Read Me` and `Methodology`. |
| D12 | **Cumulative rate products in Excel via `EXP(SUMPRODUCT(cond × ln1p))`**, where `ln1p = IF(1+r_eff>0, LN(1+r_eff), 0)` is a helper column in `tbl_RateLog`. A `Checks` row flags any `1 + r_eff ≤ 0`, and data validation bounds filed % to [−50%, +200%] and achievement to [0%, 150%] so the guard can never trip with validated inputs. | Classic-mode Excel has no conditional `PRODUCT`. The log-sum identity is exact, blank-row-safe (blank ⇒ ln 0 = 0), and keeps every formula under the length target. Each `IF` guard is documented; `IFERROR` is avoided entirely. |
| D13 | **The Solver's base index `W⁰` uses `taken` rows only** — it solves for a single planned action that *replaces the entire planned program* at the chosen date. | Acceptance §11.7 requires that solving for the worked example's own CY LR return +5.0% at 4/1/2027 — only possible if the +5% planned row itself is excluded. "Replace the planned program with one action" is the cleanest defensible reading of "a single unknown planned change," and is stated prominently on the `Solver` sheet. |
| D14 | **Scenario definitions**: each scenario applies four levers to the *base* inputs of the selected combo — Δ filed points added to every planned row, an effective-date shift in months (via `EDATE`), an achievement override, and a Δ to `M_1` (and `M_2`, so the post-P segment shifts with it). Blank levers = base value; scenario engine blocks live on `_calc`, presentation on `Scenarios`. | Matches §5.9's levers ("planned change magnitude, effective date, achievement, and mod path") with the fewest inputs, applied uniformly so scenarios stay auditable. |
| D15 | **Control's scenario selector** picks which scenario (Base/S1–S4) is surfaced in the Control KPI row (side-by-side with Base); the main engine sheets and Bridge always show Base inputs. | Letting a scenario silently rewire the visible audit path would undermine traceability; the Scenarios sheet is the sanctioned comparison view. |
| D16 | **Control's "LR basis display" toggle** switches the Projected-LR KPI between "as input" and "at current rate level"; the underlying basis remains the per-combo input in `tbl_LR` and the Bridge always normalizes from that input. | §4 makes basis a per-combo input; a global override would corrupt combos entered on different bases. A display toggle preserves the §5.2 feature without that hazard. |
| D17 | **Mod adjustment**: effective switch = per-combo `tbl_LR` toggle AND the global Control master toggle. Same pattern for seasonality (global on/off × per-LOB weights, blank row = uniform). | Both §4 (per-combo/per-LOB inputs) and §5.2 (Control toggles) are honored; AND-composition is the only non-surprising semantics. |
| D18 | **`formula_mode: modern` is validated but not implemented** (config raises a clear error). Classic ships as default and only classic is generated. | §8 says modern "may" be offered. A half-exercised second formula dialect doubles the audit surface for zero methodological value; declining it is the auditable choice. Documented in README as a future hook. |
| D19 | **Attribution actuals**: per planned row (up to 8 slots, auto-pulled for the selected combo), the user enters actual achieved change % and actual effective date; plus actual mod anchors (`M_0ᵃ`/as-of and `M_1ᵃ`, defaulting to plan) and actual CY LR. Decomposition order: (1) rate magnitude at planned dates, (2) rate timing at actual dates, (3) mod drift, (4) loss-side residual — order stated on-sheet (§7 E2 requires the order be documented). Taken rows are assumed booked as logged. | Matches §7 E2's step list; the residual absorbs everything not premium-side, and the reconciliation check proves the factors multiply back to actual. |
| D20 | **No merged cells anywhere** — titles use `centerContinuous` alignment. | §8 bans merges in calc areas; banning them everywhere is simpler and safer (merge-related range corruption is a classic workbook failure mode). |
| D21 | **Portfolio total**: two rows — an EP-weighted total over rows where plan EP is provided, and a simple average over all rows; a footnote states which rows the weighted total covers. All 18 seeded combos ship with sample EP so the weighted row works out of the box. | §5.8 asks for EP-weighted "where provided (simple average with a footnote otherwise)"; showing both makes the fallback permanent and transparent rather than conditional formatting sleight-of-hand. |
| D22 | **Sample data**: all 18 BU×LOB combos are seeded with clearly-marked sample rows; `BU-A | Property` carries the exact §9 worked example and is the default selection. Inland Marine demonstrates the 6-month term override plus a seasonality profile; one combo (`BU-C | Umbrella`) ships with *no* rate-log rows to prove the degenerate path computes factors of 1.000 cleanly; one combo demonstrates achievement < 100%, and one a mid-month effective date. | §4 wants one realistic example row per table; §10 wants the term override demonstrated; §3.5 requires degenerate inputs to compute cleanly — seeding one live degenerate combo keeps that permanently exercised. |
| D23 | **Oracle-tie checks are fingerprint-scoped**: `Checks` compares the workbook's live Bridge to `_oracle` constants only while the worked-example inputs are still present (a fingerprint formula watches the relevant input cells); otherwise those rows report N/A — INPUTS CHANGED. Structural checks (matrix sums, bounds, identities, sanity) are always-on. | Baked constants are only meaningful for the inputs they were computed from; failing loudly on *edited* inputs would train users to ignore red. |
| D24 | **Python source is split** into `src/engine.py` (oracle), `src/build_workbook.py` (generator core + CLI), `src/xlstyle.py` (theme helpers), five focused sheet-builder modules (`sheets_inputs / sheets_engine / sheets_calc / sheets_main / sheets_report`), plus `tools/recalc.py` and `tools/verify_workbook.py`. The 48-cohort engine block is emitted by ONE shared writer (`sheets_engine.write_cohort_block`) everywhere it appears — visible Rate Engine, all 18 `_calc` combo blocks, scenario/attribution/solver variants — so the audited formula pattern is identical throughout. | §10 names the two core files; a single 4,000-line generator would be less auditable, which violates the brief's own tiebreaker. One block writer eliminates the classic failure mode of parallel formula implementations drifting apart. |
| D25 | **Sheet protection not applied** (allowed as optional by §8); stated on `Read Me`. | Protection without passwords adds friction for the maintaining team while providing no integrity guarantee; the Checks panel is the integrity mechanism. |
| D26 | **Solver seed target** is the worked example's own monthly-convention CY LR at full precision, so the shipped workbook demonstrates acceptance §11.7 (returns +5.000% at 4/1/2027) on first open. | Self-demonstrating sample beats a blank input. |
| D27 | **Flow Dashboard "unearned runway"** (§5.7e) is defined as `W_eoq / E_QTD_q − 1`: the written index at quarter end versus the index earned during that quarter — the premium-rate level already locked in on the books but not yet showing in earned premium. Definition printed beside the table. | The brief names but does not define the quantity; this is the standard "carryover runway" planners quote, and it is computable from displayed columns. |
| D28 | **Uniform seasonality normalization**: seasonality weights are used as relative weights (normalized to mean 1 in-engine for display); all engine ratios are normalization-invariant. A blank seasonality row or the global toggle OFF ⇒ uniform. Weights validated ≥ 0, sum > 0. | §3.2.4. Ratios cancel any scale; validation prevents zero-sum degeneracy. |
| D29 | **P+1 mod anchors**: `M_2` defaults to `M_1` (flat beyond 12/31/P) per §3.1; the kink at 1/1/(P+1) is after every cohort mid-month that CY P earns from, so the CY P linear-mod identity is unaffected. Identity check auto-suspends (reports N/A) when its preconditions fail (M_prior supplied and off-line, seasonality on, or T ≠ 12). | §3.3.5 scopes the identity to "default settings"; suspending with an explanation beats false FAILs. |
| D30 | **Rate entry format**: percent-formatted fractions everywhere (5.0% stored as 0.05), consistent across filed %, achievement, trends, LR. Stated on `Read Me` and enforced by validation ranges. | §12 default. |

*(Log continues in §5 below as further calls arise during the build.)*

## 4. Environment facts recorded at build time

- Windows 11, Python 3.14.6, openpyxl 3.1.5, pytest 9.1.1, PyYAML 6.0.3.
- LibreOffice absent; Microsoft Excel 16.0 present with COM automation (pywin32) — used for
  headless recalculation. `tools/recalc.py` auto-detects Excel COM, then `soffice`, and reports
  clearly if neither is available (README carries the first-open checklist regardless).

## 5. Further decisions made during the build

| # | Decision | Rationale |
|---|----------|-----------|
| D32 | **Excel defined names are case-insensitive** — the generator's name registry rejects any two names differing only by case. Found the hard way: `slv_w` (weights) and `slv_W` (index) silently collided, making the Solver read weights where it wanted the index; renamed to `slv_widx` and guarded permanently. | Excel resolves `slv_W` and `slv_w` to the same name; without the guard the failure is silent and data-corrupting. |
| D33 | **Recalculation via Excel COM** (`tools/recalc.py`), LibreOffice fallback coded and auto-detected. The shipped workbook is generated by openpyxl, then recalculated and saved by desktop Excel so it carries cached values and Excel-normalized parts; it is never round-tripped through openpyxl after that. | LibreOffice absent on the build machine (§9 permits an equivalent recalc script); Excel is the authoritative engine. The one-way pipeline avoids openpyxl re-serialization of Excel-written structures. |
| D34 | **Checks scoping for per-BU files**: the "Solver Mode B April row equals base CY LR" cross-check presumes the seeded single +5% @ 4/1/P planned program and is emitted only when that pattern exists in the file's seed (it is absent from some per-BU outputs). Mode A's round-trip check is universal and always emitted. | A check that FAILs on a structurally valid file trains users to ignore red. |
| D35 | **Verification harness scope** (`tools/verify_workbook.py`): beyond §9's requirements, the harness ties all 18 combos to per-combo oracle runs, drives every toggle (mod master/row, basis, seasonality+term, all 18 selector states, scenario levers, solver inputs incl. mid-month dates and 80% achievement, attribution actuals, blanked rate log, plan-year change) through real Excel recalcs on scratch copies, and statically proves the absence of merged cells, volatile/dynamic-array functions, IFERROR, and external references. Final state: 272 checks, 0 failures. | "Zero formula errors under every toggle state you can exercise" (§9) is only credible if the states are actually exercised through the real calculation engine. |
| D37 | **State dimension (user direction, 2026-07-30).** The user's planning process is one workbook per line of business, with loss ratios and rate changes maintained by state and business unit. Restructure: each generated workbook covers ONE LOB; the selector key is `BU\|State`; tbl_LR carries one row per BU × state (3 × 21 = 63 seeded rows plus configurable spares); tbl_RateLog scales to 240 rows; tbl_Seasonality is keyed by STATE (renewal concentration such as coastal books is a state-level phenomenon here; blank row = uniform); the policy term becomes a single workbook-level input (`nr_TermMonths` on Inputs) since all combos share the workbook's LOB — Inland Marine's file demonstrates the 6-month term across its whole book; the config gains a `states:` list and `table_capacity:` block, and `output.lobs` replaces the old master/per_bu modes. Sample data is generated deterministically per combo (no randomness — regeneration is reproducible), with the §9 worked example seeded at `BU-A \| <first state>` in every workbook, and each workbook baking its own oracle constants (the 63.36% headline appears in annual-term books; sub-annual books tie their own baked values). **`src/engine.py` is untouched** — the oracle is combo-agnostic. Verification: full harness on the Property file (104 checks: all toggles, selector subsample, scenarios/solver/attribution, plan-year change), phases A–C on every other file (35 checks each, including all 63 combos × 9 metrics tied to per-combo oracle runs; Inland Marine's pass is the term-6 coverage), pytest 59/59. | User-directed dimensional change; keying and capacities follow their stated workflow. |
| D38 | **State Summary exhibit + trend default (user direction, 2026-07-30).** New flagship `State Summary` sheet (after Portfolio): one row per state — adjusted plan EP, mods (M_ind/M_0/M_1), the combo's rate-change history in chronological date/percent column pairs (up to four, plus a count), engine results (CRL_ind, E_CY, A_rate, earned mod, A_mod), and the CY P and CY P+1 plan LR calculations — with a **business-unit filter whose "All" view combines BUs on adjusted-EP weights** (states without EP fall back to a simple combo average; rate-history columns appear in single-BU views because BUs run different programs; the # chgs column counts in every view). Aggregation runs through short SUMIFS over metric×EP product columns added to `_calc`'s results table. Also added: a **Control-level default net trend for P+1** inherited by any combo whose tbl_LR trend cell is blank (an explicit per-state entry — including 0 — always wins; blank-vs-zero is distinguished with ISBLANK), and the EP column is relabeled **adjusted plan EP** — it is the weight behind Portfolio totals and all State Summary aggregation, per the user's confirmation that adjusted EP is what they weight by. | User-requested exhibit; the per-state/EP-weighted duality is exactly the Portfolio semantics reused, so both views stay provably consistent with the engines. |
| D39 | **Net rate selection (user direction, 2026-07-30; strictly optional, per BU×state).** The dilemma: product sometimes declares an OUTCOME — "we get x% net (rate + schedule-mod price) on business written from 1/1/P; specifics TBD" — rather than a datable program. Entering that as a rate-log row is wrong because log entries are level steps that compound on history (their example: +10% filed 10/1/(P−1) plus a modeled "+10% at 1/1" would stack to +21% in January). **Interpretation adopted**: the selection is a year-over-year renewal target on the combined net price index `q(k) = W(k) × M_w(k)/M_ind` — for cohorts written on/after 1/1/P, `q(k) = q(k−12) × (1+x)`, superseding planned rate rows and the projected mod path *for those cohorts only*; history before 1/1/P earns exactly as modeled (the carryover half of CY P). This reading makes the fall filing the delivery vehicle for Jan–Sep renewals (their expiring policies pre-date it) and — deliberately, visibly in the `Q net` column — compounds on Q4 renewals of already-increased business, which is the literal meaning of an x% net target on renewals. The rejected alternative (a level target vs. some baseline) is exactly a planned row / the Solver, and contradicts "we want to get x% *in* 2027". Mechanics: three columns in every engine block (`q hist`, `Q net` via a 12-rows-up self-reference — no circularity — and `index in force`); every aggregate reads the in-force column, so OFF is bit-identical to classic; the bridge shows one combined factor `A_net = CRL_ind/E[q]` with `A_mod = 1` (rate and price are inseparable under a net target); P+1 has its own optional selection (blank = carry x); with the combo's mod adjustment OFF the target degrades to rate-only (advisory-flagged). Solver and Scenarios' planned-row levers describe the explicit-program counterfactual (on-sheet notes); a fifth scenario lever varies Δ-net; Attribution suspends its premium-side steps for net-mode combos (mixing net and explicit bases would make the factors meaningless). Oracle: `net_sel_p/net_sel_p1` on ComboInputs, monthly recursion + a continuous closed-form analog (history breakpoints shifted +12/+24 keep Simpson exact across the self-referencing path), 10 new tests including the user's exact example. | The user's stated workflow, implemented as an outcome assertion rather than a synthetic rate row — auditable, strictly optional, and exact against the oracle in both conventions. |
| D40 | **Paste-friendly inputs, YoY price chart, Solver relabeling (user direction, 2026-07-30).** (1) The Inputs datasets are now CONTIGUOUS paste blocks: tbl_LR inputs occupy A:R and tbl_RateLog inputs A:H with the BU\|State key and every engine helper column moved to the RIGHT of each block — nothing (key formulas or otherwise) interleaves the user's paste range, so one dataset pastes straight over each table (data validation remains on the cells; pasting passes over it). All named ranges repoint; every formula reads columns by name so nothing else changed. (2) Flow Dashboard's quarterly exhibit now shows **YoY earned RATE and YoY earned PRICE change side by side** (price = rate index × quarterly earned mod / M_ind — the combined net price; under a net selection the series coincide by construction), fed by a new mod-numerator row on the Rate Engine. (3) The Solver's Mode B is relabeled as **timing sensitivity** — headers, chart title, and an explicit note now state that each point is the *full-year* plan LR under a different start month for the same change (later start = less earned benefit), not the loss ratio evolving through the year, which the previous labeling invited readers to assume. | All three are usability corrections raised by the user; none changes any computed value (verified: full harness still 122/0, all combos tie at 1e-9). |
| D41 | **State Summary documentation panel (user direction, 2026-07-30).** Since the State Summary is most readers' first stop, the calculations are now documented directly beneath the exhibit for a general audience (actuaries and product managers): the bridge in one line, a plain-language column guide (what CRL_ind, E_CY, A_rate, A_mod, the P+1 view, and net selections each mean and which direction they push the plan LR), and a **live worked example** whose seven steps pull from the Control-selected combo via named ranges — so the example always shows real current numbers, survives sample-data replacement, and adapts its own wording when a net selection or proposed-basis LR is in force. | Self-documenting exhibits beat external manuals; live values can never drift from the workbook they explain. (Incidental fix: cell text may not begin with "=" — openpyxl stores it as an invalid formula and Excel refuses to open the file.) |
| D42 | **Live dimension lists (user direction, 2026-07-31).** The BU and state validation lists were static config bakes; renaming or adding a value in tbl_LR left every dropdown and the State Summary pointing at stale names. Now `_lists` **extracts the unique BUs and states actually present in tbl_LR** with classic first-occurrence helpers (COUNTIF-over-prior-rows flag + running rank + INDEX/MATCH pull — no dynamic-array functions), and the dropdown names are self-sizing non-volatile `start:INDEX(...)` ranges. tbl_LR's own BU/State dropdowns become **suggestion-only** (non-blocking DV) since that table *defines* the roster — pick an existing value or type a new one; the rate log, Control selectors, seasonality table, and the State Summary filter stay blocking against the live lists. The State Summary's state column reads the live roster too and carries three spare rows for states added in-book. A roster-integrity advisory flags duplicate BU×state combos. The verification harness gained an in-book rename exercise: rename a BU and a state across tbl_LR and the rate log, and assert the live lists, the selectors, and a newly-grown State Summary row all follow and tie the oracle. Static enumerations (status, Y/N, basis, toggles, scenarios) rightly remain static. | The brief's own workflow is "rename to real values"; the workbook should follow an in-book rename without regeneration. Lift was moderate: the only nontrivial piece is unique extraction under classic-mode constraints. |
| D43 | **Plain-language label sweep (user direction, 2026-07-31).** Symbol-first labels (`M_0`, `M_1`, `A_mod`, `CRL_ind`, `E_CY`, `C_pre`, `D …`) read like a spec, not an input form. Every user-facing label now leads with plain English and keeps the technical symbol in parentheses so cross-references to Methodology, the named ranges, and the engines survive: tbl_LR headers ("Current avg written mod (M_0)", "Projected mod at 12/31/2027 (M_1)", "Apply mod adjustment?", "In indication? (Y/N)" …, with plan-year-aware wording), State Summary headers and its documentation-panel terms ("Rate earn-in (A_rate)", "2027 earned rate level"), Portfolio headers, Control summary rows, Bridge input labels and the basis step ("Convert to current rate level ×(1+s)"), Scenario levers (the ambiguous "D filed rate" → "Extra filed rate on planned rows (+/- pts)"), Solver outputs ("Old-rate earned exposure before the date (C_pre)"), the Rate/Mod Engine cohort column headers, and the quarterly table on the Flow Dashboard. Header rows got taller for the wrapped two-line labels. No formula, name, or address changed — labels only (verified: full harness 130/0 unchanged). | Inputs should be fillable by a product manager without a legend; symbols stay for auditability. |
| D44 | **Live plan-year labels (user direction, 2026-07-31).** The user's back-testing workflow (set Control's plan year to the prior year and reproduce last year's answer) exposed that while every CALCULATION follows the plan-year input (all engines are name-driven; the harness ties plan-year changes to the oracle at 1e-9), many labels had baked the generated year as static text — so exhibits looked "not updated." Fixes: (1) year-bearing presentation labels are now **live formulas** (`="CY "&nr_PlanYear&" plan LR"`) on the State Summary headers and group captions, Portfolio headers, Bridge waterfall headers, Control/Scenario metric labels, and the Rate/Mod Engine header labels; (2) input-table headers and chart titles/captions became **year-neutral** ("Projected mod, end of plan yr (M_1)", "plan year shaded") since Excel table headers cannot hold formulas; (3) a **Control banner** appears whenever the plan year differs from the generated year, stating that calculations follow the input while sample data and some prose still describe the generated book; (4) the harness gained a set-back-a-year exercise asserting values AND live header text both follow, and (5) the §3.3.5 identity check's tolerance now scales with the mod path's daily slope (floor 0.0002): shifting the plan year back compresses the sample's 0.860→0.890 ramp into ~3 months, and the monthly-discretization residual of the identity is proportional to slope — the fixed tolerance was calibrated to the brief's gentle example, not an error in either engine. | Back-testing against a prior year is a first-class workflow; labels must not lie about what was computed. |
| D45 | **One-click selection navigation (user direction, 2026-07-31).** The single Control-tab selector drives every combo view, which forced tab-hopping to change BU/state. True synchronized dropdowns on every sheet would require VBA (banned by the brief) or legacy form controls (which openpyxl cannot emit and regeneration would wipe), and independent per-sheet selectors would let exhibits disagree — breaking the single-source audit trail. Solution within the rules: every selector-driven sheet (Rate Engine, Mod Engine, Bridge, Flow Dashboard, Scenarios, Solver, Attribution) carries a frozen-header "Selected: BU\|State" echo plus a **HYPERLINK "Change BU/state selection >"** jumping straight to Control's selector, and Control row 4 carries a **jump-bar** of links back to all ten analysis sheets — a two-click round trip with no tab-hunting. HYPERLINK() is classic-mode, non-volatile, and LibreOffice-safe. | Selection stays single-source (auditability preserved); only the navigation cost was removed. |
| D36 | **Workbook-audit fixes.** A 24-agent six-lens audit of the generated artifact (sheet compliance, formatting, input architecture, acceptance criteria, documentation, actuary-UX walkthrough; 2-vote adversarial verification per finding) confirmed four defects, all fixed: (1) **major** — every chart axis rendered hidden in Excel: openpyxl omits the `<c:delete>` element when `axis.delete` is `None`, and Excel's resave during headless recalculation interprets the omission as *deleted*; all axes now set `delete=False` explicitly (the Flow Dashboard plan-year band's primary value axis stays hidden by design), y-axis titles added while in there, and the verification harness now asserts post-resave chart XML has exactly one hidden axis; (2) the labeled seed-pattern rate-log row (BU-C \| General Liability, planned 6.5%) now matches that combo's selected indication change `s` = 6.5% so the §4.2 pattern actually demonstrates what its comment claims; (3) date validation added to the `M_0 as-of` column of tbl_LR (the Checks panel still enforces the before-12/31/P bound); (4) the Snowflake / Power Query input-refresh future hook is now noted on Methodology and in README alongside the other three §7 hooks. | Charts that lose their axes after the very recalculation step that ships them are exactly the class of failure only an artifact-level audit catches — the fix is guarded by a permanent harness check. |
| D31 | **Adversarial-review fixes (oracle).** A 21-agent independent review (3 lenses + 2-vote adversarial verification per finding) confirmed 6 defects, all in solver/validation periphery — the §9 worked example was independently re-derived from the brief text alone with zero mismatches. Fixes applied: (1) the Solver closed form now inverts the engine's exact D4 blend — when a taken change shares the effective month, the effective-month cohort contributes `(1−p)·W0_pre` to C_pre and `p·W0_eom` to C_post with `p` measured at the *first* in-month change date (previously it split at the input date on the blended index, mis-solving by up to 0.07 LR pts); (2) `C_post = 0` (effective date after all CY-P-earning cohorts, or concentrated seasonality) now returns NaN with the §7 E1 guard-rail warning instead of crashing; (3) zero-earned-exposure reported months (possible under term < 12 + concentrated seasonality) now emit an engine warning — the workbook's monthly `E_m` cells already show blanks, never `#DIV/0!`; (4) the out-of-window validation message now distinguishes pre-window changes (fold into the base index) from post-window changes (never earn; affect `CRL_ind` only if flagged considered); (5) Attribution's mod-drift factor uses the **plan** `M_ind` — an indication property — against the actual earned-mod path, so a stray "actual M_ind" cannot manufacture drift; (6) Attribution rejects actuals that match no planned row. All six are locked in by new pytest cases. | Correctness first; each fix keeps the Excel implementation and the oracle in exact agreement under the same conventions. |
| D46 | **Presentation architecture v3 (user direction, 2026-07-30).** A three-persona review (senior pricing actuary, product manager, Excel template architect) drove a presentation overhaul with zero engine changes: (1) **prose physics** — row heights computed from the HOST column's width (`xlstyle.text_height/prose`), never a fixed chars-per-line guess; explicit heights suppress Excel auto-fit, so an under-estimate clips text invisibly (60-75% of the Methodology/Read Me prose was invisible); (2) **answer-first Bridge** — claim sentence + waterfall above the fold, the 20-cell nr_* resolver demoted to an outline-grouped audit block (free re-layout: the harness binds Bridge only by name), sentinels rendered as text in COMPANION cells so the numeric cells engines consume are never texted, chart category labels pointed at live cells; (3) **monthly cadence** — YoY earned rate/price charted monthly (quarterly bars hid mid-month steps the engine already computes), new unearned-runway chart, and a carryover ledger decomposing E_CY(P+1)/E_CY(P) by action with an interaction residual reconciling exactly to nr_YoY_P1; (4) **live input echoes** — tbl_LR V:Y and Rate Log V:Y show each row's combo plan LR / delta / factors so edits move the answer on the same row; parameters sit ABOVE the tables; (5) **Read Me as orientation hub** (hyperlinked guide, canonical glossary, quick start) + Methodology TOC and a Rate Engine column dictionary; (6) **Portfolio Decision Board** — top-10 adverse movers (epsilon-tiebroken LARGE/MATCH), EP-weighted contribution + tornado, portfolio bridge with an explicit mix/interaction residual (factor averages do not compound exactly), autofilter with a filter-don't-sort note; (7) **Checks stay append-only** — new rows append at the tail, the category roll-up lives in COLUMNS I:K, every row carries a "Fix at" hyperlink; (8) One-Pager (print-ready VP brief) and Compare (any two combos, served from calc_*) sheets; final tab order runs orientation -> inputs -> all-combo answers -> selected-combo chain -> what-ifs -> audit -> docs. Charts reading outline-HIDDEN staging rows must set `plotVisOnly=False` or Excel plots nothing. | Presentation debt was the workbook's biggest adoption risk; every change is verifiable because nothing moved that the harness pins by address. |
| D47 | **Rate Log split (user direction, 2026-07-30).** The rate change log moved to its own sheet: identical A:H paste block, DV, and capacity; helpers J:S unchanged plus a new planned-rank helper (`rl_prank`); its own frozen header; live V:Y columns (combo plan LR via MATCH on lr_key, delta pts, % of the plan year the action earns — exact via the selected combo's Rate Engine block, and an achievement-trap warning). rl_* names re-registered against 'Rate Log'; the _calc scenario/attribution transformed logs re-reference the new sheet; harness mutation targets follow SHEETS.RATE_LOG. | The user's stated pain — "plug in dates and changes and see the change immediately in the same table" — needed a same-row feedback loop and a correct freeze, both impossible while the log lived under tbl_LR. |
| D48 | **Hybrid selection sync (user direction, 2026-07-30).** Sync is not just audit: the selected-combo resolver cells physically feed the visible engines, and the Checks cross-ties prove visible engine = hidden _calc block for that combo. Model: ONE global selection drives the deep-dive chain; the **Solver alone** gets a local override (Follow-Control? Y/N + BU/state dropdowns resolving `slv_key`; the solver base block re-keys on it, and a local resolver serves CRL/LR_cur/A_mod/A_other from the all-combo calc_* results — zero new engine compute; its oracle ties go N/A when not following). Side-by-side viewing is a DATA feature: the Control compare block and the Compare sheet read calc_* directly. An input cell cannot default via formula (overtyping destroys it), hence Y/N + overrides rather than "formula until touched". | Filing conversations use the Solver standalone; everything else stays one-selection so every printed page agrees. |
| D49 | **Walkthrough sheet (user direction, 2026-07-30).** The fully worked example, live for the Control selection: inputs echo with per-field HYPERLINK to the exact Inputs cell and honest blank rendering; chronological CRL_ind build-up with the cumulative considered product; THREE specimen cohorts (carryover / first mid-month-change / straddle) with the split-p blend recomputed from its ingredients beside the engine's value; E_CY aggregation; the mod path with anchors as real dates, slope in pts/month, and the Jan-1 identity; factor assembly into `nr_WalkLR`; two auto-composed "say it upward" sentences; and a reconciliation strip proving this tab = Bridge = hidden engine = baked oracle. nr_WalkLR must tie nr_CYLR_P (in-book check + harness tie). Scenarios also ship with S1 seeded (+2 pts on planned rows, harness-tied) so the tab demonstrates itself, and the scenario chart plots CHANGE VS BASE in pts. | Recomputation is the pedagogy exactly where the method is hardest (the mid-month split); everywhere else the tab references, never recomputes — so it can never drift from the exhibits it explains. |
| D56 | **The dual-module Layout trap (found by the harness, 2026-07-30).** `python -m src.build_workbook` loads the module as BOTH `__main__` and `src.build_workbook` — two separate Layout classes. `build()` configured `__main__`'s Layout while the sheet builders read the canonical module's, silently falling back to class-body defaults; the bug stayed invisible for as long as the defaults happened to equal configure(shipped config), and bit the moment D47 changed configure() without them (the log built at rows 85+ while names and harness expected 7+ — phase-D mutation exercises caught it; phases A-C could not, because a self-consistent wrong geometry still ties the oracle). Fixes: the `__main__` guard delegates to the canonical module's main(), and a permanent test asserts class defaults == configure(shipped config). | A verification harness earns its keep on exactly this class of bug: everything looks right until you mutate the workbook at the rows the code CLAIMS it used. |
| D57 | **Net Delivery decomposition (user direction, 2026-07-30).** For combos on a net rate selection, PMs need the delivery story: how the target lands month by month, and the filed-change + pricing combination that satisfies it. The engine's own definition factors exactly — delivered(m) = q(m)/q(m-12) = [W(m)/W(m-12)] x [M(m)/M(m-12)] (M_ind cancels). New "Net Delivery" tab (after State Summary) + hidden `_netcalc`: (1) the rate leg's base is the **LIVE rows** — taken plus planned effective before 1/1/P (D39 semantics; deliberately NOT the Solver's taken-only D13 base), with a dedicated live-filtered log strip whose first-in-month flags are recomputed (rl_first is filter-unaware); (2) one new change r at the **state's own effective date** (rate timing varies by state; per-state date inputs, blank = a default-date control; dates must lie inside the plan year — a prior-year vehicle also changes the year-ago base and the closed form stops being exact) enters each month **affinely**, W_new(m) = A(m)+B(m)r with the D31 first-in-month day-blend at the earlier of D and the first live in-month change — never the multiplicative shortcut, which regresses the D31 bug when D's month is co-occupied; (3) the suggested filed change solves the **written-basis average** in closed form (r* = ((1+x)SUM w - C0)/C1, mods on the already-projected path), and the **dual solve** turns the pricing ask into one actionable number: plan-month mods are affine in M_1 along the anchor path, so the year-end mod M_1' also solves in one SUMPRODUCT (a PM can type it into tbl_LR); (4) the signature exhibit is the **twin state x month grids** — the YoY rate leg on renewals, and the required YoY pricing change that satisfies the target (heat-mapped); (5) honesty rails: 'All' view and non-net combos dash out everywhere; mod-adjustment-OFF renders the target rate-only per D39; the written-basis solve is disclosed against the earned shape via the booked-program plan LR reconciliation; P+1 is out of scope (its year-ago base is itself the asserted path). Oracle: `net_delivery_components / suggest_net_rate / required_m1 / net_delivery_by_month / net_program_plan_lr` (+31 tests incl. affine-vs-brute-force across a D-grid with a taken-co-occupied month). Checks: max solve residual, live-vs-full-log year-ago identity, the engine's own T(m)/T(m-12)=1+x self-check, and a feasibility-flag advisory — all appended at the tail. Harness: phase-C ties (r*, M_1', booked-program LR, all 12 grid months at 1e-9) plus five phase-D exercises (net-on worked example, mid-month date, co-occupied month + override, master-mod OFF, All-view fabrication guard). | The tab answers the PM's actual question — "given the rate change date and current flow, what pricing gets us to the target" — with the same 1e-9 oracle discipline as everything else; every convention choice (live rows, affine month-of-D, written-basis objective) exists because the naive alternative ships wrong numbers. |
| D58 | **Solver base-block first-flag defect (found via the D57 design review, fixed 2026-07-30).** The taken-only solver base block filtered its SUMIFS by status but read the status-BLIND `rl_first` flag: a PLANNED row effective earlier in the same key+cohort month as a TAKEN row stole the flag, the block's first-change day count L collapsed to 0, split p = 0, and the cohort valued at W_pre — silently dropping the taken change from its own effective month (the oracle's `_solver_components` filters to taken rows FIRST and was correct; reproduced at 9.2bp of required rate on a scratch mutation before fixing). Fix: a status-aware `rl_firsttaken` helper on the Rate Log (first change among the key+month's TAKEN rows) and the solver block's `LogRefs(first="rl_firsttaken")`; the shared cohort writer is untouched. Audit of the other filtered logs: the scenario/attribution transformed logs recompute their own first' columns over their own rows (filter-consistent), and the D57 `_netcalc` live strip already carries its own live-aware flag — only the solver block read a mismatched flag. Locked in by: a pytest oracle pin (closed form vs brute-force bisection of the D13 counterfactual with mixed months in P-1 AND in the solve month itself, 1e-9) and a harness phase-D exercise that mutates the Rate Log to a planned-early/taken-later month and ties nr_SolverR at 1e-9 through a real Excel recalc. | Filtered blocks must never borrow helper flags computed under a different filter — the third instance of this lesson (scenario logs D-era, ndl_first D57, now the solver); the writer now gets the flag injected per filter via LogRefs. |
| D59 | **Program Flow (user direction, 2026-07-31).** John asked for the DESCRIPTIVE twin of Net Delivery: "an analogous flow tab that just shows the flow for normal rate and mod change." Shape (agreed after weighing merge-vs-separate): a full merge into the Flow Dashboard would mix two selection models on one tab (the dashboard follows Control; the grids are BU-scoped), so the breadth exhibit gets its own tab and the combo-scoped depth strengthens the dashboard. (1) New "Program Flow" tab (between State Summary and Net Delivery): BU picker, per-state summary (adjusted EP, net-target echo, w-weighted plan-year average rate/mod/delivered legs, delta vs the net assertion) and THREE state x month grids — YoY written rate leg, mod leg, and delivered net (heat-mapped) — all reading the EXISTING _calc combo blocks via the blessed stride-INDEX: the raw written index (col N) and the anchored mod path (col O) are program-basis even in net mode (supersession lives only in S/T/U), so the grids cost no new cohort machinery; the per-block results rows gained three SUMPRODUCT ratio-average cells (H/I/J). (2) **Program basis doctrine**: every log row enters AS LOGGED — taken at filed %, planned at filed % x achievement; net-mode supersession deliberately NOT applied — for net combos the gap between program-basis delivery and the asserted (1+x) is the exhibit's point, and Net Delivery prescribes what closes it (advisory + cross-links both ways). (3) The Flow Dashboard's monthly detail gained the same written-basis legs for the selected combo PLUS the **locked leg** — the rate leg on taken rows only, served by a new taken-only cohort block keyed on nr_SelKey with the status-aware rl_firsttaken flag (D13 + D58; the solver base cannot be reused because slv_key can detach from Control) — the planned residual ("your July +8% is only +3% locked"), a delta-vs-net-assertion column (x for P months, x1 for P+1), and a delivered/rate/locked line chart. (4) Averages are w-weighted means of monthly RATIOS (the Net Delivery delivered-average convention), pinned per-block so the tab's summary is one INDEX per cell. Oracle: `program_flow_by_month` (24 YoY months, locked leg via filter-FIRST changes_override; 8 tests incl. the D58 mixed-status month on the locked leg and a net-mode-invariance pin). Checks (tail): locked block == solver base when the Solver follows Control (two independent emissions of the same taken-only math — the cross-tie that would catch a D58-class wiring bug), dashboard avg delivered == _calc block results, and a >=1pt program-vs-assertion gap advisory; _fix_target gained pf_/fd_/nd_ routing. Harness: six phase-C tie groups (dashboard M..Q x 24 months, locked block x 48 cohorts, every live state's summary averages, the worked-example grid rows) and five phase-D exercises (appended planned row, master-mod OFF, mixed-status month on the LOCKED block, All-view fabrication guard, net-combo delta columns). | Fills the one flow question nothing answered — "what is my actual program delivering, state by state, month by month?" — at near-zero engine cost because D57's machinery already carried the ingredients; the locked/planned split is the actionable nuance PMs argue about, and it is exactly the D13 filter made visible. |
| D60 | **All-BU combined Program Flow (user direction, 2026-07-31).** John: "seems like it would be tough to see flow for all business units combined when one is not selected." The 'All' dead-end was mechanical, not conceptual — grid cells INDEX one combo's block, and a combined view needs a dynamic SET of blocks, which classic formulas cannot address. Solution: extend the D38 flat-projection architecture from annual scalars to monthly data — **each combo's 12 monthly delivered/rate/mod legs, EP x seasonality-weight products, mod-adjustment flag, average-ratio echoes, and net-gap are published as columns AM..CM on the `_calc` results table** (exported FLOW_PUB map = the single source of truth for the tab and harness; all strictly numeric because SUMPRODUCT *-form aggregation errors on text), so any cross-BU aggregation is one SUMPRODUCT. **EP is the exactly-correct within-state weight**: seasonality is keyed by STATE (D37), so same-state combos share identical weight columns and w cancels out of the combination — verified on both the workbook and oracle sides. Under 'All' the summary shows EP-weighted means of the per-combo average ratios (net target follows Net Delivery's netx-mean convention) and every grid cell EP x w-weights the state's BUs; new **IN VIEW total rows** on all three grids give the BU/book-level monthly flow line (view condition purely multiplicative, `(pf_BU="All")+(calc_bu=pf_BU)` — no IF over arrays). Honesty rails: combined legs are weighted MEANS, so rate x mod need not multiply to delivered exactly under mix — delivered is the exact statistic (disclosed on-tab, Methodology 6d, the Portfolio-bridge precedent); the delta-vs-assertion column stays per-BU ("—" under All) because targeting is per-combo; a **view-independent Checks advisory** watches every net combo's program-vs-assertion gap book-wide via the published gap column (the v2.3 view-scoped advisory stays, append-only). Oracle: `combined_flow_by_month` (ComboInputs.plan_ep's first consumer; 5 tests incl. seasonality-cancellation equivalence and mix-effect documentation). Harness: 3 phase-C published-column tie groups (all combos x 12 delivered legs, EP x w products, avg echoes) and the All-view exercise REWRITTEN from a fabrication guard to full oracle ties (summary, 2 states x 3 grids x 12 months, the IN VIEW book row) — a deliberate behavior change. **Pattern-reuse evaluation (John's ask)**: adopted — the All view, IN VIEW rows, book-wide gap monitoring, a Portfolio jump to Program Flow; deferred — a Portfolio book-delivered chart and Compare-tab monthly strips (both become trivial reads of the published columns if ever wanted); rejected on principle — Net Delivery 'All' (a filing suggestion averaged across BUs is meaningless; the guard IS the feature) and Flow Dashboard scope (D48); rejected on cost — cross-book scenario impact (needs 69 x 4 scenario blocks). | The blocked view was an addressing problem, not a math problem; publishing the projection once makes every future cross-combo exhibit a one-SUMPRODUCT feature while keeping the audit trail (each published cell is a static link into the block it summarizes). |
| D61 | **State Summary legibility: change status + visible bridge arithmetic (user direction, 2026-07-31).** Two user findings on the flagship exhibit. (1) *"It is impossible to tell which rate changes are taken, which are planned, which are included in the indication."* True: the four chronological slots showed only a date and an effective percent — and because the percent is filed % x achievement, a planned row at 80% achievement displayed a number matching neither the filing nor anything else on the sheet, with no cue why. Each slot gains a narrow **status token** (`T`/`P`, plus a trailing `*` when the row is outside the indication), reinforced by conditional formatting (planned reads amber italic) but never dependent on colour — the token prints. The single "# chgs" column becomes **# taken / # planned**, which unlike the slots works in the All view too, where the per-change columns are necessarily blank (they are single-BU): the leadership view previously carried zero program information. (2) *"For the plan loss ratios, instead of doing lookups, why can't we just do the math directly in the table — aren't the components already there?"* Almost: the chain is LR_current x A_rate x A_mod x A_other and only **A_other** was missing from the exhibit (it existed per-combo on `_calc` but was never surfaced; a new EP-weighted product column `calc_w_aother`, appended after the D60 flow block so nothing moves, supports its All-view average). The plan-year group is reordered into the bridge read left to right — Projected LR | x Rate earn-in | x Mod drift | x Other adj | = Plan LR — and **the plan LR cell is that product whenever the row resolves to exactly one combo** (`AJ`=1), which covers every single-BU row and every All-view state served by one BU. The reason it could not simply always be the product is the mix effect: where a row aggregates combos, each factor shown is an EP-weighted average and the product of averages is not the average of products, so those rows keep the exact EP-weighted engine value and a new **Mix (pts)** column carries the residual — the same honesty convention as the Portfolio bridge (D46) and the Program Flow All view (D60). Guard: a per-row helper compares the visible product against the ENGINE's weighted value on single-combo rows (not against the cell itself — that would be tautological), rolled into a `SUM(ss_mathchk)` Checks row at 1e-9, plus a phase-C tie on the Mix residual and phase-D token/product assertions. Also renamed Program Flow's ambiguous "IN VIEW" rows: the summary band is a **TOTAL**, the per-grid roll-ups are **BOOK AVG / BU AVG** (live, following the picker) with each grid note stating they are EP x weight averages of the state rows above, not totals. | An exhibit that can be audited by multiplying across the row beats one that has to be trusted; and status was the one fact the exhibit hid that changes how a reader treats every number in the row. The mix residual is what makes "show the math" honest rather than merely convenient. |
| D62 | **Config-driven sample seeding + duplicate-dimension guard (found when the user renamed the book, 2026-07-31).** John renamed `business_units` from the shipped `BU-A/B/C` to his real units (`ABD`, `MM`, `SBA`) and reshuffled the state list. Generation crashed: the sample seeder and the harness addressed the seeded worked example and showcase combos by LITERAL BU NAME (`next(r for r in lr_rows if r["bu"] == "BU-A" ...)` -> StopIteration), so the very first thing a new user is told to do — rename the config to their own book (D37/D42) — broke the generator. Fixed by addressing business units POSITIONALLY through `_bu(cfg, i)` (clamped to the roster) at all 48 sites across `src/build_workbook.py` and `tools/verify_workbook.py`; the worked example is now "the first configured BU x the first configured state" whatever they are called. Second finding from the same edit: the new state list contained **IA twice**. A repeated dimension value does not fail the build — it silently corrupts it: `BU|State` keys stop being unique, every MATCH resolves to the first of the pair, the live roster (which extracts UNIQUES, D42) runs one row shorter than the configured count, and only the duplicate-combo Checks ADVISORY (a WARN) hints at it. `load_config` now raises a one-line error naming the repeated values, and the harness de-duplicates the configured states when walking exhibit rows (`_states(cfg)`) so a config error reports as a clean FAIL instead of a TypeError off the end of the roster. Verified: with the user's real BU names and a de-duplicated state list, a full harness run is 190/0 including every phase-D toggle exercise. | The rename path is the first thing every real user takes, so it must be the best-tested path, not the one nobody exercised since the sample was written; and a dimension typo should stop the build in one legible line rather than survive as a WARN buried in a 60-row Checks panel. |
| D63 | **Net Delivery adopts the planned filing as its default (user direction, 2026-07-31).** John: *"The net delivery tab uses a suggested change. Instead of or in addition to that can we bring in planned rate change in the plan year to use as the default."* Adopted as **in addition to**, which turns the tab from a hypothetical into a decision aid: with both input columns blank it now takes the PLANNED plan-year filing already sitting in the rate log — its date, and its EFFECTIVE percent (filed x achievement, what actually earns) — while the closed-form suggestion stays on the exhibit beside it, so the gap between what the PM intends to file and what the target would require is visible in one row. Precedence per state: typed override -> planned filing from the log -> default date + suggested change (unchanged when no planned filing exists). This is deliberately re-introducing a row the engine suppresses: under a net selection, planned rows on/after 1/1/P are superseded (D39), which is exactly why they were invisible here — the tab is where one is put back to ask "if I book the filing I am already planning, how much does pricing still have to carry?". No new math: the decomposition already takes an arbitrary (D, r), so the pickup is pure input plumbing. Mechanics: the dead `rl_prank` helper (nothing read it) was repurposed into **`rl_firstplanned`** — 1 for the key's first planned row inside the plan year — computed under exactly the window and status its consumer filters on, per the D58 lesson; `_netcalc` block headers gained the planned count / date / effective % / filed % cells that the date and override resolvers now fall back to. Only the FIRST plan-year filing is adopted, because the model carries one change at one date and compounding two onto one date would misstate the D31 blend; states with more are flagged **MULTI-PLANNED** (picked up by the existing feasibility advisory, so no new Checks row). The tab shows the adopted filing's date and filed % in two new columns, reports "none" where the log has no planned filing, and the sample now seeds a planned filing on the net-showcase combo so the pickup demonstrates itself on first open (it changes no plan LR — net mode ignores those rows). Oracle: `planned_change_in_plan_year` (+2 tests, incl. achievement handling, earliest-wins, and a brute-force check that adopting the filing reproduces the same delivery); harness: 2 phase-C ties (change in force and date equal the planned filing) and 2 phase-D exercises (move the planned row -> the default follows and M_1' re-solves at the new date; delete it -> the tab falls back to the suggestion and reports "none"). | The planned program is the PM's actual intent and it was already in the workbook — defaulting to a hypothetical filing instead made the tab answer a question nobody asked. Showing both is what makes the exhibit a decision: intent, requirement, and the distance between them. |
| D64 | **Net Delivery deep-dive band regression + the tie that was missing (user-reported, 2026-07-31).** John: *"the one combo under the microscope section on the net delivery tab doesnt seem to be working."* Correct, and self-inflicted the day before: the D63 band resolvers were built with `('...$PD{t}...').format(t=t, PD="X")` — `PD` is not a placeholder in that template, so the literal text `$PD` survived and the band's effective date resolved against column **PD** (empty) instead of **X**. The date came back 0, fell outside the plan year, the in-window flag went to 0, and every band figure degraded to "n/a". Fixed by building both resolvers as f-strings. The deeper problem is that a whole exhibit block was **unverified**: the band had no oracle tie, so a formula pointing at an empty column produced a workbook that still passed 198 checks. Added a phase-C cross-tie — the band resolves the SAME combo through its own pickers, so its 12 monthly rate/pricing rows and its change in force must reproduce that state's block cell for cell (1e-12), plus an assertion that the band's resolved date lands inside the plan year. Writing that check immediately caught a second, independent error in the check itself: the band block is `block_top(len(states) + 3)`, not `block_top(len(states))` — the roster spares sit between the last state and the band, so the first draft was reading an empty spare block. | Two lessons, both old ones: a formula built by string templating needs the same review as the formula it produces, and an exhibit with no tie to the oracle is an exhibit that can silently stop working — "it recalculates without errors" is not the same as "it is right". |
| D65 | **Program-basis plan LR alongside the asserted net path (user direction, 2026-07-31).** John: *"wouldn't it be cool to calculate the projected loss ratio with the net rate path in addition to a separate rate and mod path?"* — then, asked where it should live: *"state summary would be the highest value place, but would it be easy just to make it visible everywhere."* Both, and everywhere was cheap for the reason D60 established: publish the number ONCE as flat columns on the results table and each additional surface costs a column, not machinery. A net selection replaces the premium side with a single asserted combined factor (A_mod forced to 1, D39), so the combo's explicit valuation — its logged rate program earning in, times its projected mod path — simply is not computed anywhere today. It now is: two cells per engine block (the earned rate index recomputed on the WRITTEN path, col N, instead of the in-force path, col U, for P and P+1), then `calc_cylr_prog / calc_cylr1_prog` = LR_current x (CRL / that index) x the UNFORCED mod drift x A_other, plus EP-weighted products for aggregation, a signed gap column, and an identity column. Surfaced on **State Summary** (two columns beside 'Net rate sel', EP-weighted, dashed where no assertion exists — the group caption becomes "Net selection — asserted vs the logged program"), **Portfolio** (per combo; its ranking helpers moved past the Decision Board to free the columns), **Program Flow** (the plan-LR twin of its monthly leg gap), the **Control KPI band** (one book-wide figure), and a **Bridge sentence** under net mode ("if you booked the logged program instead… X%, ±N pts"). The invariant that keeps it honest: a combo with no net selection has only ONE path, so program basis must equal the headline exactly — a Checks row sums that residual across every non-net row at 1e-9, which self-validates the feature on ~98% of the book, plus an advisory for net combos landing 1pt+ away. Oracle: `program_basis_plan_lr` (run the bridge with the selection removed) + a test asserting the two coincide off net mode and that the program basis is invariant to the asserted x. Harness: 4 phase-C ties (all combos both years, the non-net identity, Portfolio's row, State Summary's EP-weighted value) and a phase-D exercise switching the worked example onto a net selection so the gap opens exactly as the oracle says. | The workbook could state what a net target implies but never what the actual program delivers at the plan-LR level — so the two halves of the argument a PM has to make were never on the same page. The identity on non-net rows means the comparison cannot silently drift. |
| D66 | **The Book: one combined, filterable roll-up across every line (user direction, 2026-07-31).** John: *"we need a way to look at all lines of business at once… a separate combined workbook that has a lot of the same exhibits just not live… we should be able to filter on any combo we want… State Summary would be the highest value."* Options were weighed (a fully live 378-combo workbook — ~800k formulas and minutes to recalc; a book tab inside each LOB file — duplicated six ways and mutually stale; external links — banned outright, the harness asserts no external references; Power Query — off the classic-mode reservation). The chosen shape is a **seventh workbook harvested from the six**, because the flat per-combo projection D60/D61/D65 already built IS the book: six `_calc` results tables stacked. **Truth comes from the workbooks, not the config** — recomputing from `config.yaml` would only ever reflect sample data, so `tools/harvest.py` reads the six RECALCULATED files. Its failure modes are the design: `data_only=True` returns None for an uncached formula, so an un-recalculated source would harvest as zeros and roll up into a plausible, wrong book — the harvest refuses and names the file, and `ntaken` (a COUNTIFS, hence always numeric in a compatible file) doubles as a schema probe that catches a source built by an older generator. "Not live" means precisely: the ENGINES are frozen; every filter and aggregate is a live formula. Exhibits: **State Summary** (flagship — per state, EP-weighted, the full bridge chain, rate-change history where the filters resolve to a single combo), **Portfolio** (all 378 rows, Excel filters on the grid), **Roll-ups** (by line / by unit / by state, each with its mix residual), **Program Flow** (monthly legs), **Checks**, **Read Me** with the as-of stamp and per-source provenance. Filter doctrine: an exhibit keyed by a dimension applies the OTHER filters, never its own. Combining lines is safe for loss ratios and written-basis legs (seasonality is state-keyed and shared across LOBs; the legs are term-independent, pinned by test_term_independent) — earning fractions are never aggregated. To feed the book's State Summary, `_calc` also began publishing the four chronological rate-change slots and the taken/planned counts (the only things the per-LOB exhibit showed that a harvested row could not reproduce). Verification is its own harness, `tools/verify_book.py`: static scans, a recalc + error sweep, every harvested row tied to a per-combo oracle run, aggregates tied to Python recomputations, and five filter exercises driven through real Excel recalcs — 43/0. | The roll-up was an addressing problem, not a modelling one: publish the projection once and the seventh workbook is assembly. Harvesting from the files rather than recomputing is what makes it true for a book that has had real numbers pasted into it. |
| D67 | **Prose stored as a formula — the D41 trap, caught the hard way (2026-07-31).** The book workbook would not open in Excel AT ALL: `Workbooks.Open` failed even with `CorruptLoad=xlRepairFile`, with no usable diagnostic. Bisecting by builder stage, then by column, then by write category (patching `put`/`formula`/`header_row` to no-op per sheet) isolated it to `header_row` on State Summary — where one header label was the literal string `"=  CY 2027 plan LR"`. openpyxl stores any string beginning with `=` as a FORMULA, and `<f>  CY 2027 plan LR</f>` is not parseable, so Excel rejects the entire file rather than the cell. D41 recorded this exact hazard in 2026-07-30 and it still recurred, because the per-LOB sheet sets that header as a live formula (`="=  CY "&nr_PlanYear&" plan LR"`) while the book, having no live year, wrote it as text. Fixed by writing the header as a formula that yields the text. Permanent guard: `verify_book.py` phase A now flags any `=`-prefixed cell whose body looks like prose (contains a space, with no call, concatenation or quoted string) — a defined-name reference like `=bk_srccombos` passes, a sentence does not. | Two hours of bisection for one leading character. The lesson worth keeping is not "don't start a label with =" but that openpyxl silently reinterprets a value's TYPE based on its first character, and Excel's failure mode for that is total rather than local — so it needs an automated guard, not a remembered rule. |
| D68 | **Two years on the flow tabs — the year now flowing beside the plan year (2026-08-01).** The cohort blocks have always spanned Jan P-2..Dec P+1, so P-1 was computed on every combo and never displayed. The tabs now show it: `A | B..M = Jan P-1..Dec P-1 (steel) | N..Y = Jan P..Dec P (navy)`. Chosen over a year picker because a picker cannot be read across the boundary — and the boundary is the point: a filing appears in the left band while it earns through and in the right band only until its anniversary, so reading across the seam shows how much of today's momentum survives into the plan. Oracle: `program_flow_by_month` gains `prior_rows` + `avg_*_prior`, and `combined_flow_by_month` gains `prior_rows` + `avg_delivered_ratio_prior`, both computed by the SAME helper as the plan year so the two views cannot drift. Published as `PRIOR_PUB` (rate/mod/delivered x12 + three averages), APPENDED after `BOOK_PUB` so no published column moves and `tools/harvest.py` keeps every index it maps; `_calc` 112 -> 151 columns. Block results row gains M/N/O mirroring H/I/J. | Free data that was already being computed. Appending rather than inserting is what keeps the book harvest — and therefore the seventh workbook — untouched by a change to the per-LOB tabs. |
| D68a | **One `epw` family serves both years.** The seasonality weight is `12*INDEX(se_block,srow,MONTH(m))/ssum` — a function of calendar MONTH alone — so `w(Jan P-1) = w(Jan P)` and the published plan-year EP x w products weight the prior year correctly. Saved 12 published columns. Pinned twice, deliberately: `test_prior_weights_equal_plan_year_weights_by_calendar_month` in pytest, and a phase-C tie that recomputes `w` at P-1 month indices and compares it to the published P value. | An assumption that saves work must be cheaper to VERIFY than to avoid. Both pins fail loudly if seasonality ever becomes year-specific. |
| D68b | **The summary table moved into the two year bands rather than staying in A..I.** Excel column visibility is sheet-wide, so a summary occupying B..I would be destroyed by collapsing the prior-year months. Program Flow's summary therefore puts its prior-year averages at B/C/D and its plan-year metrics at N..T, and the prior-year months carry `outlineLevel = 1`. Collapsing removes the prior year from the summary AND the grids at once, leaving a tab identical to the pre-D68 layout. Net Delivery gets NO outline group — its summary keeps user INPUT columns (eff date, filed %) under B..M — and its Flags column moved to Z so wide flag text is never clipped by a month column. | The collapse had to be semantically whole: hiding "last year" must hide all of last year, not shred the exhibit above it. Where that was impossible without moving inputs, the group was omitted rather than made dangerous. |
| D68c | **Net Delivery shows the prior year on the rate leg only.** The required-pricing grid stays plan-year and says why: there is no target in P-1, so a required walk is undefined there — the prior-year block is deliberately empty, with a header that states it. The microscope band also stays plan-year but moved to N..Y so its months line up with the grid block above. | The tab is prescriptive; the prior year is descriptive context for the filing decision. Showing a "required" leg against a target that does not exist would be fiction, and this workbook's whole contract is that a displayed number means something. |
| D69 | **Legends drawn on top of the plot — the D36 trap again, on a different element (2026-08-01).** Every chart carrying a legend rendered with it over the series, unreadable. Cause: openpyxl's `Legend.overlay` defaults to `None`, so `<c:overlay>` is omitted — and ECMA-376 defaults a MISSING overlay to **true**. Excel therefore materialises `<c:overlay val="1"/>` on resave, which is exactly the shape of D36 (an omitted `<c:delete>` becoming `deleted=1`): an optional element left out acquires a hostile default, and the damage appears only after the round-trip, not in the file openpyxl wrote. Fixed with one shared `xlstyle.chart_legend()` that sets `overlay = False` and `legendPos = "b"`, called from `sheets_report._chart` (which covers the five Flow Dashboard / Rate Engine charts) and explicitly at the three sites that build charts directly — Mod Engine's path chart, the Walkthrough mini, and Net Delivery's stacked+line combo (called AFTER the `+=` merge so it lands on the combined chart's legend). Bottom rather than right: these are all month or cohort series, so vertical space is the cheap dimension. Note `Legend` has no `position` alias — the attribute is `legendPos`, and setting `position` would have silently done nothing. Permanent guard: phase A parses every `xl/charts/chart*.xml` and FAILs any legend without an explicit `<c:overlay val="0">`, accepting either namespace form since Excel rewrites with the `c:` prefix. | Third instance of the same failure mode (D36 axes, D41/D67 `=`-prefixed strings, now legends). The rule worth generalising: when a library models an optional OOXML element as None-by-default, do not assume the omission is neutral — check the schema default, set it explicitly, and guard it in the harness, because these only manifest after Excel rewrites the file. |
| D70 | **Stepped schedule-mod actions — the mod leg becomes something the book EARNS (user direction, 2026-08-01).** The mod path was a drift line between planner-supplied anchors: M_0 at an as-of date, M_1 at 12/31/P. That is how a mod is FORECAST, not how it is EXECUTED. A schedule-mod change is a dated percent that stays in force until superseded — the same mechanic as a rate filing — so it now has its own log (`Mod Log`, its own `ml_*` names, its own sheet). Drift owns history through 12/31/(P-1); the STEP LOG owns the plan year onward; the two meet at M_endPrior, a new input, so the path is continuous and no action is counted twice. `blended_step_index` is shared with the rate leg deliberately: the two legs are the same mechanic on different columns, and one implementation means they cannot drift apart the way two copies would. With an empty log nothing below the switch is consulted and the engine is bit-identical to the pre-D70 build. | A separate table rather than a `kind` column on the Rate Log: ~20 sites read `rl_ln1p` / `rl_seq` / `rl_status` assuming every row is a rate row, CRL_ind among them, so a mod row in that table would have to be excluded at each of them — and one missed site produces a plausible wrong on-level index. Separation makes the contamination impossible by construction rather than by audit. |
| D70a | **Logging a combo's FIRST action makes M_endPrior live — the re-anchor is not inert (2026-08-01).** Under a net selection D39 supersedes the mod leg, and the natural reading is that a mod action therefore cannot move the plan LR. I asserted exactly that, and it was wrong by 0.15 LR points. The STEP is inert; the RE-ANCHOR is not. Once the log is non-empty the drift path lands at 12/31/(P-1) on M_endPrior instead of running on to M_1, which changes every P-1 cohort's written mod. Forcing M_endPrior onto the drift line reproduced the base to 1.11e-16, confirming the mechanism. | My test assertion was wrong, not the code. Pinned as `test_under_a_net_selection_the_step_is_inert_but_the_re_anchor_is_not` so the distinction cannot be re-forgotten. |
| D70b | **A blank M_endPrior extends the drift; it does not stop at M_0 (2026-08-01).** The oracle evaluates the UNSWITCHED anchor path at 12/31/(P-1) when the input is blank; the workbook fell back to M_0, at four separate sites. About a tenth of a mod point on the sample book — small, wrong, and completely invisible, since neither number looks unreasonable. Blank means the planner has not RESTATED the level, not that the mod stopped drifting. One helper, `sheets_engine.mod_drift_at_switch`, now feeds every site. | Found while widening the stepped machinery in D75, not by any test: both sides were internally self-consistent, so only a direct oracle-to-workbook comparison could expose it. |
| D70c | **An as-of date on or after 12/31/(P-1) collapses the switch anchor (2026-08-01).** D70 moves the third anchor back to 12/31/(P-1); an as-of date at or after that lands it on top of the second, the slope denominator goes to zero, and the whole block reads `#DIV/0!`. "Mods as of 12/31 of the prior year" is an entirely ordinary thing to type. Handled the way the engine already handles it — drop the switch anchor and keep the M_prior -> M_0 line extrapolated (or a flat M_0 when M_prior is blank) — rather than validated away. Implemented by nudging x2 one day past x1 and putting m2 on that same line: a one-day segment at slope s IS the line at slope s. | Validation would have been cheaper, and would have told a user that a perfectly reasonable input was illegal. |
| D71 | **Chart and axis TITLES drawn on top of the plot — the D36/D69 trap, one level deeper (2026-08-01).** Same omitted-element mechanic as the legends, on `<c:overlay>` inside `<c:title>`: 16 chart titles and 10 axis titles rendered over the plot area, making the axis names unreadable. Fixed as a save-time sweep (`xlstyle.unoverlay_titles`) rather than at each construction site, and guarded in phase A with a prefix-agnostic scan, since openpyxl writes a default namespace and Excel rewrites with `c:` on resave. | Third instance of the same trap. The guard is what stops there being a fourth: any future title that acquires the hostile default now fails the build's own verification. |
| D72 | **Inputs column order declared once, not six times (user direction, 2026-08-01).** John asked for the mod inputs in chronological order with the indication's assumed mod first. The columns were defined by six parallel position-keyed structures — headers, keys, formats, widths, descriptions, validations — so a reorder meant six edits that all had to agree. Replaced by one `LR_COLS` tuple of `LrCol` records; every consumer derives from it, and `lr_letter()` maps a name to its column. | The reorder immediately surfaced that M_endPrior had NO data validation at all — it had been appended after the validation block was written. A single declaration makes that class of omission structurally impossible. |
| D73 | **Solve for the mod step — the same target on the pricing lever (user direction, 2026-08-01).** The Solver answered "what rate change hits this plan LR". Mod actions are the other lever, so it now answers the same question on them: required step by effective month, the directed equivalent at an achievement assumption, the share of the year still reachable, and where the feasibility cliff falls. Closed form, mirroring `_solver_components` one-for-one, solved on the c = 0 member of the stepped family so the D70 re-anchor is already in force and the linearisation is exact rather than nearly right. Mods live in [0.5, 1.5], so a rate-sized bound is meaningless on them: `nr_SolvMaxMod` is its own input. | On the sample book: to hit 64.0%, March costs +2.02% and May +3.16%, and it is infeasible from October. That comparison is the exhibit's whole purpose — the cost of slipping a date, in one number. |
| D74 | **Net Delivery leads with its chart (user direction, 2026-08-01).** John: *"the chart you render at the bottom of net delivery is actually very good and should show up at the top."* The whole deep-dive band moved with it, so the picture still sits beside the numbers that produced it. Cheap because the band is FIXED height — it does not grow with the state roster — so everything below it shifts by a constant, `ND_FIRST` stays a plain module constant, and the harness keeps importing it as a number. | A build-time assertion now fails loudly if the band ever grows into the summary, rather than silently overwriting it. |
| D75 | **The mod ask published as a dated ACTION, not only as a level (2026-08-01).** `required_m1` answers Net Delivery's pricing question with a year-end mod LEVEL, which is the right shape of answer only while the mod path is a drift line — once the Mod Log pins the path there is no free M_1 to solve for and the line reads n/a. `required_mod_step` answers it as an action: file a mod change of c at the filing's own date (one filing normally carries both levers), divided by achievement to get what you must direct. Closed form, exactly parallel to the filing solve — along the stepped path the plan-year written mod is affine in (1 + c) just as W_new is affine in r, so it falls out of the same sum with the same h(m) weights. | Its base is the mod log WHOLE, not the Solver's taken-only base (D13): a net selection supersedes planned FILINGS (D39), but a logged mod action is the pricing plan this exhibit measures the target against — the same reading the tab already takes for the projected path. |
| D76 | **Reconcile the endpoint-average process to the earned model (2026-08-01).** The shop values the mod leg by averaging the two year-end mods; the model earns it through the book; nobody had priced the difference. On the sample book the current process reads about a POINT LOWER than the plan year actually earns — it credits more mod relief than arrives. The exhibit rests on an identity rather than an argument: the endpoint average is EXACT for one action on 1/1, a twelve-month term, and a flat prior year, so every departure from it is attributable. Three legs, ordered least to most actionable — TERM (the book turns over in under twelve months), TIMING (actions dated later than 1/1), HISTORY (the carry-in drifted up to the start level rather than sitting at it) — tied to the gap at 1e-9. | The first cut had two legs and a theta contaminated by history, which came out NEGATIVE on a live combo. Every test passed; the flat-history fixture hid it completely. Only running the finished function against the sample book exposed it. Theta is now measured on a flat-prior-year counterfactual and history is its own leg. |
| D77 | **The pricing leg gets the rate leg's locked / planned split (2026-08-01).** "How much of this month's YoY is already done" was answered for rate and not for pricing. Since D70 a mod action is a dated percent exactly like a filing, so it has to answer for both legs or neither. The step index filters to TAKEN mod rows, but the D70 regime switch and the level the steps compound on follow the PRESENCE of a Mod Log, not the taken subset: filter both and a combo whose log is all-planned loses the re-anchor, the two legs land on different year-ago bases, and the residual reports the re-anchor as though the planned action had caused it. An all-planned log therefore reads a flat locked path on the level the year actually starts from — nothing done yet. | The change also corrected the delivery chart, whose dashed "locked" line was the RATE leg plotted against a rate x mod solid line, under a caption promising "what survives if no planned action lands". It dropped the whole pricing leg. It is now locked DELIVERED. |
| D78 | **Excel's AND and SUMPRODUCT do not short-circuit (2026-08-01).** `IF(AND(ISNUMBER(x), ABS(x)>k), ...)` evaluates `ABS(x)` even when `ISNUMBER(x)` is FALSE, so a cell holding "n/a" poisons the result with `#VALUE!`; `SUMPRODUCT((a<>"")*(ABS(b)>k))` does the same on every element regardless of the mask. Both bit during this release — the Net Delivery flag string under the mod-master-OFF toggle, and the mod reasonability check over blank Mod Log rows. Numeric tests are now NESTED inside their own `ISNUMBER`, and range tests run on columns that are numerically 0 on blank rows (`ml_ln1p`, which is monotonic in the change, so the bound maps exactly). | Both were caught by the harness's zero-formula-errors scan under a toggle exercise, not by reading. The pre-existing RATE-BOUND term had the same shape and worked only because its cell happened never to go n/a — it was rewritten too rather than left armed. |
| D79 | **Mode C charts the loss ratio, not the required step (user direction, 2026-08-01).** John: *"The solver chart for the mod gets a scale that is silly ... wouldn't it make more sense to mimic the rate chart with loss ratio on the y axis instead of mod."* He was right, and the cause is structural rather than cosmetic: the required step is (Mbar x D - K_pre)/K_post - 1, and K_post goes to zero as the year runs out, so the series is a hyperbola. On the sample book it ran +3.4% in January to +493% in December (directed: +4.9% to +705%), forcing an axis that put the eight months anyone would act in inside the bottom 4% of the plot. The chart now does what Mode B does for a filing: it TIMES one chosen action and traces the full-year plan LR as its date slips, against the flat target. Same book, same combo: 63.33% in January to 64.41% in December — a 1.1-point range, with the target crossed in January. | Charting the SOLVED step's LR consequence would have been useless: by construction it lands on the target every month, so the curve is a flat line. That forces the chart to time a FIXED action instead, which means it answers a different question from the table beside it — and that is exactly the Mode A / Mode B relationship, one lever over. Both reads are now reported, because they fail differently: a step can be perfectly fileable and still too small to reach the target, or big enough and no longer fileable. |
| D80 | **The trust panel becomes paste-proof, and warnings reach the front tab (2026-08-01).** An adversarial UX review found the documented workflow — paste your book over Inputs, the Rate Log and the Mod Log — quietly defeating its own guardrails. Excel PASTE replaces the target's data validation with the source's, so DV only ever guards the first typed entry; the Checks panel was named as the real integrity mechanism but mirrored 9 of ~20 DV bounds, and every input-sanity row used COUNTIF/N() forms that skip text. The engines guard blanks with N(x)=0 — and N("0.95") is also 0 — so a CSV-origin paste SUBSTITUTES a neutral value: paste M_0 as text and the plan LR quietly loses the mod adjustment with every check green. Eighteen rows added: ISTEXT hygiene per paste block (the only thing that can see a text-typed number), the eight unmirrored bounds, key-helper consistency on all three tables (a one-column paste overshoot lands exactly on the cell every engine block resolves through), the mod leg's missing status/achievement pair, a far-end date typo guard, an EP unit-error tripwire, and a tripwire for seeded SAMPLE mod actions. Separately, the status banner counted FAIL only, so any number of WARNs left Control green — including a duplicated roster row, which double-counts EP in every weighted total while INDEX/MATCH views show only the first. Three states now: ALL CHECKS PASS, PASS WITH n WARNING(S) in amber, CHECKS FAILING: n. The shipped sample reads amber out of the box, which is honest — it carries one standing advisory. | Duplicate roster rows escalated WARN -> FAIL (a broken roster is not advisory). The engine-window date check splits: the far end is now FAIL because a 2072-for-2027 slip clears the 1990-2100 validation and can never be intentional, while the near end stays advisory because an old filing kept as history is legitimate. Harness gates on 'no FAIL' via checks_pass() rather than matching the banner string, so warnings never fail a build. |
| D81 | **One mod-adjustment default, not two (2026-08-01).** A blank or misspelled 'Apply mod adjustment?' token read as ON in the visible chain (the Bridge turned it off only for a literal "OFF") and as OFF in the hidden one (_calc, the Solver and _netcalc turned it on only for a literal "ON"). So a token cleared by a paste made Portfolio and State Summary silently drop mod drift for combos the Bridge still adjusted, and only the SELECTED combo tripped an existing structural check — the other 68 diverged in silence. Unified on ON-default through a single mod_adj_on() helper used by all five sites, matching engine.ComboInputs.mod_adjustment_enabled (True) and the visible chain; sample_to_combo's Python mapping moved with it. | The right answer to a malformed token is not a better default but a louder complaint, so a token-validity FAIL row lands with it. Five template sites, not the three the review first counted — a per-block A_mod pair in _calc was hand-synced too, which is exactly how the two conventions drifted apart in the first place. |
| D82 | **Regeneration keeps your book, or refuses (2026-08-01).** build() constructed a brand-new Workbook seeded with sample data and saved it over the existing file: no read, no merge, no backup, no prompt. Any regeneration at an unchanged plan year — and config.yaml explicitly invites 'add/remove freely ... capacities scale automatically on regeneration' — destroyed every pasted cell (4,508 input-styled cells per workbook, ~27,000 across six), with git as the only recovery path and that documented nowhere. Now: the build refuses to overwrite a workbook holding real inputs unless you pass --force or --carry-forward, writes a timestamped .bak.xlsx before any overwrite, and --carry-forward reads tbl_LR, the Rate Log, the Mod Log, seasonality and the Control scalars back out of the existing file and rebuilds around them. | Everything is read BY DEFINED NAME (src/carry.py), never by cell address: the names are the workbook's own stable contract and this reader must work against files built by an OLDER generator than itself. Nothing it reads is a formula, so carry-forward works on a file saved from Excel seconds ago — no recalculation required. The sample-detector compares real input VALUES against freshly generated seeds rather than trusting a baked flag (the _oracle fingerprint does not move when a user pastes), and a file it cannot parse reads as NOT-sample: refuse, never guess. A carried build also bakes its oracle constants from the user's own first combo, so the Checks oracle ties finally apply to a real book instead of reporting N/A forever — and the SAMPLE banners flip to CARRIED FORWARD, because a banner that lies about real rows is how a real row gets deleted. |
| D83 | **The book will not roll up a failing workbook (2026-08-01).** tools/harvest.py never read a source's own Checks status, so a LOB whose trust panel was red — a bad paste tripping the mod-bounds or EP rows — harvested cleanly into a Book whose own Checks panel passed. The Book is the leadership artifact and carried no signal that a source was broken when read. Harvest now reads each source's Checks!C3, refuses a failing one (with --allow-failing-sources as the escape hatch), records the banner on the Source record, shows it in a 'Source checks' column on the Book's Control sheet, and counts it in a hard Book Checks row. verify_book gained a freshness phase: external links are banned so nothing IN the file can notice a source that moved on after the harvest, but the comparison data already sits on both sides — the Book stamps each source's mtime — so the harness does what the workbook cannot. | The Book's own Read Me was routing users into D82's trap: 'To refresh: regenerate the LOB workbooks...' — regeneration is both unnecessary for an input change (a plain save updates the caches harvest reads) and, until D82, catastrophic. Rewritten to the actual weekly loop: save in Excel, rebuild the book. The checks gate is ordered AFTER the cached-value gate so a never-recalculated file keeps the more specific message. |
| D84 | **Half the cohort log scans were computing an answer the row above already had (2026-08-01).** Every cohort row ran two sweeps of the rate log: "index before this month" (eff < start of month) and "index at month end" (eff <= end of month). But the block's own calendar sets B(r) = EOMONTH(B(r-1),0) + 1, so eff < B(r) and eff <= C(r-1) select the identical rows — the second row of every pair was recomputing the first's answer. Rows after the first now read $J(r-1) directly (and the mod leg its own twin); 9,635 of 19,758 EXP(SUMPRODUCT) sweeps disappear, measured. Separately, blocks that resolve to a BLANK key — the six spare tbl_LR rows, and every _netcalc block while Net Delivery is on the All view — now short their four scan columns to the constants the sweep provably returns for an empty key (index 1, counts 0), which Excel's lazy IF then never evaluates. Verified value-identical: a full rebuild of the sample Property book differs in ZERO of 168,051 non-empty cells. | The measured payoff is smaller than the finding implied, and worth recording honestly: a full CalculateFullRebuild of the whole workbook goes from 0.39s to 0.36s — about 9%, which is exactly what the verifier's cells-touched model predicted (8.9%) and is also 9% of an interval nobody notices. The workbook was never slow; 171k classic formulas rebuild in under half a second. This ships because it is free, provably identical, and makes the cohort block say what it means ("index before this month IS last month's closing index") — not because anyone was waiting on it. One precondition became a Checks row: a log row carrying a date but no BU or state has a blank key, which is what an idle block resolves to, so it would leak into blocks that must match nothing. It is a half-finished paste either way and the engine ignores it, so it is now a FAIL. Also bounded the last three whole-column INDEX ranges in the _netcalc band checks, the pattern D68 removed from Program Flow. |
| D85 | **The Solver's mod inputs were a fourth copy that had dropped its guards (2026-08-01).** The same combo mod inputs are resolved in four places: the _calc combo headers, the Bridge's nr_* cells, the _netcalc block headers — all blank-guarded, so a combo with empty mod fields reads as a flat 1.000 path — and the Solver's base block, which read INDEX(lr_m0,...) and INDEX(lr_m0asof,...) raw. For a combo with blank mod inputs (legal everywhere else) Mode C's mod leg therefore ran off M_0 = 0 and an as-of date of serial 0, silently, while every other exhibit showed the flat path. Fixed by pointing the Solver's four cells at the SOLVE combo's own _calc header cells, which removes the copy rather than re-syncing it. | Drift between duplicated derivations, not a deliberate difference: the block's own comment justifies keying on slv_key rather than the Control selection, and says nothing about dropping the guards. Values move only in the blank-input state, which the oracle cannot even represent (ModInputs has no blank m0), so the 1e-9 ties are untouched — confirmed by a zero-difference full-workbook comparison. |
| D86 | **One Excel process per run; mutations stay deterministic (2026-08-01).** Profiling a single phase-D exercise on the sample Property book: 13.9s, of which the Excel recalculation is 6.7s and FOUR AND A HALF SECONDS are an openpyxl round trip — load the workbook, assign four cells, re-emit 171k formulas — performed immediately before handing the same file to Excel, which then opens it again. The mutations now go in through COM on the way through Excel's own open, and one Excel process is reused for the whole run instead of being spawned and quit per call (~50 times per LOB phase D, ~300 per release). The COM mutation was then REVERTED and only the shared process kept — see the note. | Two failures on the way in, both worth recording because neither announced itself. (1) Caching the Excel object across CoInitialize/CoUninitialize cycles: COM objects belong to the apartment that created them, so the proxy went stale partway through a run and raised AttributeError on Excel.Application.CalculateFullRebuild — the apartment now lives as long as the instance. (2) Writing dates as datetimes: pywin32 puts a naive datetime through local-timezone conversion, shifting it by the UTC offset and sometimes onto the PREVIOUS DAY. Nothing crashed; the harness simply returned 18 failures, every one a fourth-decimal-place drift in an exercise that mutates a date (day-blends, solver effective dates, attribution actuals) — the workbook computing the right answer for the wrong day. Dates are now written as Excel serial numbers, which is what Excel stores and what openpyxl wrote, with a pytest sweep over every day of a plan year to keep it that way. The harness found both; that is the argument for running it rather than reasoning about it. (3) And the one that decided it: under the parallel release driver at six concurrent Excel instances, COM came apart — two lines raised 'The interface is unknown', and one returned a full set of assertion failures whose ACTUAL values were the workbook's DEFAULT state, meaning its mutation had silently not been applied. A harness that can quietly fail to set up its own exercise could as easily report a false PASS, so the writes went back to openpyxl (in-process, deterministic) and only the recalculation crosses into COM, where a failure raises rather than lies. The ~31% per-exercise saving was not worth a silent-setup-failure mode in the one thing the whole project trusts. Two guards remain from the episode: every mutated cell is re-read after the recalculation and a mismatch aborts the exercise, and the release driver caps concurrency by TIER — two for --full, four for --quick. Even three was too wide for phase D: a line was killed outright, with a non-zero exit and no output at all, because each exercise loads a whole recalculated workbook (865k cells) into openpyxl on top of an Excel instance per line. Verified afterwards on its own: that line passes 286/0. The driver now reports a no-output failure as what it is rather than printing an empty string, and sweeps scratch directories a killed run could not clean up. |
| D87 | **A release is one command, and the lines verify in parallel (2026-08-01).** A full regeneration was nine manual invocations — build, six recalcs (recalc.py takes one path), build_book, recalc the book — and both builders ended by printing a note telling the user to go run the next one. Skipping a LOB recalc at least stops the harvest; skipping the book rebuild is SILENT, and the book then shows last month's numbers under a fresh-looking as-of stamp. tools/release.py does the whole pipeline in tiers (--smoke = pytest, --quick = build/recalc/book/phases A-C, --full = adds phase D), running lines as parallel subprocesses that each own an Excel instance and a scratch directory, and refusing to start when a ~$ lock file says a workbook is open in Excel. | Phase D also stopped rebuilding states it already had: several exercises apply byte-identical mutation dicts (the master mod toggle is switched off by three of them, the degenerate combo by both the selector loop and its own exercise), so each distinct state is now built once and reused — every assertion still runs, only the rebuilding is shared. The selector subsample was cut from 14 picks to about six, chosen by what makes a combo different to the LIVE resolver (other-adjustment, mod anchors, a net selection, logged mod actions, proposed basis, mod adjustment off) rather than by grid position, because phase C already ties every combo through _calc and the picks were re-proving one formula path fourteen times. The round trips saved went into depth instead: each pick now ties the whole factor chain (E_CY, earned mod, A_rate, A_mod, both years' plan LR) where it previously tied the headline alone. |
| D88 | **Excel's own save strips the calculation settings the generator set (2026-08-01).** The build sets calcMode=auto and fullCalcOnLoad; every shipped workbook nonetheless carried a bare <calcPr calcId="191029"/>, because Excel rewrites that element on save. It matters because Excel's calculation mode is a SESSION property taken from the FIRST workbook opened: an actuary who opens a manual-mode model first and this workbook second can edit an input and watch nothing move — every KPI, the plan LR and the ALL CHECKS PASS banner all displaying stale cached values. With TODAY()/NOW() banned and no VBA, nothing inside the file can notice. Both attributes are now re-asserted after each recalculation. | Patched at the zip level rather than through openpyxl, deliberately: a round trip would discard the cached values the recalculation just produced, which is the one thing the recalc tool exists to create. Every other zip entry is copied through byte-for-byte (pinned by a test). The harness skips the patch for its throwaway scratch copies, which are read once and deleted. A Read Me line names the symptom and the escape hatch (Ctrl+Alt+F9), since a session-level setting is not something the file can fully own. |
| D89 | **A formula that does not close its parentheses makes the whole FILE unopenable, and Excel will not tell you which cell (2026-08-02).** Adding a headline band to the Solver, I typed one closing paren too many into a four-line f-string. Nothing complained: the build wrote the file, `openpyxl` re-read it without objection, and 282 tests passed. The failure arrived from the RECALCULATION step, ninety seconds later, as `(-2147352567, 'Exception occurred.', (0, 'Microsoft Excel', 'Open method of Workbooks class failed', ...))` — which names no sheet, no cell, and no defect, and reads exactly like an environment problem. I checked for orphaned Excel processes and lock files before suspecting my own change. Excel does not repair-and-warn here; one malformed formula anywhere in a 171k-formula workbook and it refuses the document. So `xlstyle.assert_formulas_balanced` now runs at the end of both builders (LOB and Book), walks every formula cell, and raises with the sheet, the cell and the first 160 characters. Quote state is tracked, because Excel string literals legitimately carry unbalanced parentheses — every `"(pts)"` caption and every `TEXT(x,"+0.00%;-0.00%")` mask — so a naive counter would fire on perfectly good formulas. It costs about a quarter of a second on a full build against a ninety-second round trip that ends in a lie. The generalisation, and the reason this is a decision rather than a bug fix: this project's formulas are ASSEMBLED, from nested f-strings with escaped quotes inside Excel string literals inside Python string literals — so a paren typo is not a rare event, it is an ordinary one, and the only reason it had not happened before is luck. Anything the build can prove about its own output, it should prove before Excel gets the chance to be unhelpful about it. |
| D90 | **The flagship exhibit did not show its own headline, and its column order was six parallel structures keyed by position (2026-08-02).** The State Summary read State, premium, mods, then FOURTEEN columns of rate-change chronology, then the engine levels, then the bridge — so the CY plan loss ratio, the number the exhibit exists to produce, sat at column 27, about 1,600px in. On a 1080p window with the state roster frozen it was off the right edge: the leadership exhibit required scrolling to reach its own answer, while the space above the fold went to four dated rate changes that are reference rather than conclusion. Reordered to **State, EP, mods, engine levels, the bridge through to Plan LR and its Mix residual, THEN the chronology, then the P+1 view and the net-selection block**. Plan LR now sits at column 13, about 915px, and the bridge reads left to right ending in the number it produces — which is also how the Walkthrough, the Bridge tab and the Portfolio's own footnote already describe it. Nothing was added or removed; the exhibit is still 35 columns wide. **The reorder was not the hard part.** The order lived in six places that had to agree by position — the header list, the width list, the two-tier group spans, the metric map, the slot arithmetic (`6 + j * 3`), and every hard-coded letter in the bridge chain, the conditional formats, the Book's independently maintained mirror of the same 35 columns, and eleven reads across the two verification harnesses. Moving one column meant editing all of them in lockstep, and a miss does not fail the build: it produces a correct-LOOKING exhibit with one column of the wrong thing in it, or a harness that goes on multiplying four cells which are no longer the bridge and still reports PASS. So the order is now declared once, as `SS_COLS` — a tuple of `SsCol(key, header, width, group, live)` — and everything derives from it: `ss_c(key)` / `ss_l(key)` for positions, `ss_groups()` for the two-tier spans (a run of adjacent equal captions), `ss_group_starts()` for the divider rules. `sheets_book` imports the same tuple rather than keeping its copy, overriding only the captions and the three year-bearing headers, because the book has no live plan year to interpolate and its honesty rule is one dimension deeper. Both harnesses read through `ss_l` too. This is `LrCol` (D72) applied to the exhibit that D72's own lesson was written about, and the cost of the next reorder is now one permutation of a literal list. Four tests pin it: the map is well-formed and its helper columns sit strictly right of the exhibit, the group runs are contiguous and no caption is split across two runs, the Book shares the identical object, and — the point of the whole change — Plan LR still fits inside the first screen. |
| D91 | **Starting a chart axis at zero is not neutral — on a levels chart it spends the whole plot proving loss ratios are positive (user direction, 2026-08-02).** John: *"i think thats a great chart, but starting at 0 when the loss ratios are going to be 50-70% seems silly. it shows up on the one pager as well. same type of issue on cumulative written vs earned rate index on the flow dashboard tab."* Right on all three. Excel's automatic minimum for a value axis is zero, which is correct for QUANTITIES (premium, counts) and wrong for LEVELS. The Bridge waterfall walks 65% to 63.4%; against a zero floor it drew five columns of visually identical height with the entire story inside the top 2% of the plot area. The Flow Dashboard's cumulative rate index runs 1.00 to 1.22, and the gap between the written and earned lines — which IS the chart, it is the rate still to earn in — was a hairline. Five charts now frame their axis on their data: the Bridge waterfall, its One-Pager twin, and the Flow Dashboard's rate index, mod path and price index. `xlstyle.zoom_axis` pads the range by 10% (never less than one point) and rounds outward to a round step, so Property's bridge gets 55%–75% and the walk fills about 60% of the plot instead of 4%. **The awkward part, and why this is a decision rather than a tweak: an Excel axis bound is a STATIC NUMBER, not a formula.** It cannot follow the data, and every one of these exhibits is live for the Control selection — so the window has to cover not just the worked example but every combo the reader can switch to. `chart_axis_windows` therefore runs the oracle once per combo at build time (about 100ms against a build that already takes a third of a second) and takes the extremes over the whole book. That still leaves the case the rest of this project refuses to leave open: a paste that moves the book outside the baked window plots off the edge, and an empty chart looks like a broken file rather than a stale axis. So the bounds are baked as named constants on `_oracle` (`axw_lr_lo` … `axw_price_hi`) and two appended Checks advisories compare the live values against them — the first over every combo in `_calc`, the second over the selected combo's plotted series — each naming `--carry-forward` as the fix. Verified by mutating six projected loss ratios to 145%: the advisory reports 18 out-of-window values and the banner escalates to PASS WITH 3 WARNINGS. |
| D92 | **Excel's teardown outlasts its Quit(), and the book step is always the one that pays (2026-08-02).** Three different COM failures hit the same step in one session — `The interface is unknown`, an RPC error, and a proxy that answered a property read with the wrong type (`'bool' object is not callable`). All three were the BOOK recalculation, which is the step that always follows a fan-out: `release.py` recalculates the six lines four-at-a-time, and each subprocess returns as soon as it has called `Quit()`, while the process itself lingers for a second or two releasing its RPC endpoints. Starting the seventh instance inside that window fails in a different way each time, which is exactly why it read as three unrelated bugs. `recalc.py` already retried and discarded the suspect instance (D86), but its backoff was 2s then 4s — shorter than the drain — so all three attempts landed inside the same window and the retry logic was, in effect, decorative. Two changes: `release.py` now waits for the Excel process count to reach zero (polled via `tasklist`, 60s cap, and it says so if it gives up) before starting the book, and the recalc backoff is quadratic — 3s then 12s — so a retry is actually a retry rather than three tries at the same instant. **That was not the whole story.** A later `--full` run failed the book step again with the drain guard in place and ZERO Excel processes running, and a direct COM probe in a fresh interpreter came back perfectly healthy moments later — so the fault is not purely a teardown race, it is genuine intermittency in Excel's COM layer that no amount of in-process retrying clears. What does clear it is a NEW PROCESS: the same book verification failed twice and passed on the third attempt, unchanged. So `release.py` retries the book steps in fresh processes (`_run(..., retries=2)`), and a LINE that was killed rather than failing an assertion — an access violation under memory pressure, which reports a non-zero exit and no output at all — is retried once serially before the release is called red. Only killed lines: a line that genuinely failed an assertion has produced output, and re-running it would just fail again more slowly. The first version of this fix covered the book's BUILD and not its VERIFY, which cost a whole 37-minute run to discover — the two steps sit in different halves of the driver and only one of them got the argument. **The actual root cause, found later:** an Excel instance that never exits. A run printed `(note: 1 Excel process(es) still shutting down; continuing anyway)` and then failed all three book attempts against it; killed by hand, that process had an EMPTY main-window title — a headless COM instance with no document, unmistakably one of ours. So it was never purely a teardown RACE: waiting helps when a process is merely slow and does nothing when one is wedged, which is the case that was actually failing. `_await_excel_drain` now TERMINATES stragglers after waiting, and does so safely by snapshotting Excel PIDs before any subprocess launches and only ever killing ones that appeared during the run — an Excel the user already had open, with unsaved work in it, is never a candidate. Two earlier diagnoses of mine are withdrawn: that pywin32 misresolves the zero-argument void `CalculateFullRebuild` as a property (tested; does not reproduce), and that waiting for the drain was sufficient (half right). Both came from reading the error STRINGS — four different ones — when the signal was in the process table, named by a line I had written myself and then read past. |
| D93 | **The plan loss ratio is the average of a race, and the exhibit that says so is worth a residual (user direction, 2026-08-02).** John: *"the loss ratio flow would be icing on the cake. trend would have to be built in though. the exhibit essentially become a race between trend and rate and mod earning. the formula would be something like lr*(net trend^1/12)*monthly earned rate adj*monthly earned mod adj. it would be net trend because trend is separate from rate and mod earning."* Exactly right, including the reason for NET trend. Every other exhibit in the workbook reports the plan year as one number, which is the correct answer for a plan and hides what a pricing actuary actually manages: the loss ratio does not sit at 63.4% all year. January renews against a rate level that has barely earned in, so it opens at 66.3%; December closes at 60.9%. The plan is the average. **The trend anchor is the one real modelling choice, and it is provable rather than tasteful.** Writing the month's trend factor as `T^((mm - 6.5)/12)` with `mm` the month NUMBER from Jan P = 1 puts the anchor at 7/1 of the plan year, and 7/1 is the unique anchor at which the plan-year exponents average to exactly 0 — so the walk averages to a headline that carries no trend — AND the P+1 exponents average to exactly 1, so next year's walk averages to a headline that carries exactly one trend step. Any other anchor breaks one of the two. The trap is half a month wide: month CENTRES average to 6.0 and month NUMBERS to 6.5, only the second is the 7/1 boundary, and the slip costs ~19 bp of factor in P and ~480 bp in P+1. A Checks row pins it (`SUM(lrf_delta_p)` = 0, `SUM(lrf_delta_p1)` = 12) because the symptom of getting it wrong is not an error, it is every month being slightly off. **The weighted mean of the walk does not equal the CY headline, and the exhibit does not force it to** (the D46 convention). It sits about 0.6 bp away on the worked example, and that gap decomposes EXACTLY — reproduced to 1e-15 in every regime — into three named terms: the rate x price covariance (+1.60 bp: the headline divides by two SEPARATE averages, `E_CY` and `Mbar_CY`, while the walk divides month by month by their PRODUCT), Jensen convexity (+0.99 bp: `E[T^d] > T^E[d]`), and a centre-of-gravity tilt (0.00 bp for an annual term at ANY seasonality, because earned exposure per month is uniform — and materially non-zero for a short term with lumpy writings, which is the exhibit telling you the fixed anchor no longer sits at your exposure centre). A `Structure` check enforces that the three terms reproduce the mean, because a residual that is not accounted for has stopped being an explanation. Weights are PREMIUM (`den x E x Mbar/M_ind`), not exposure: exposure weights alone miss by 3.4 bp against 0.6 bp. **The exhibit's headline number is the BREAKEVEN TREND** — solved in closed form from January's and December's earn-in factors as `(A_jan/A_dec)^(12/11) - 1`, the net trend at which the two months land together. 9.89% on the worked example. Above it the year deteriorates as it runs, below it improves, and the banner says which in a sentence. A net selection breaks even at its own target (5.000168% against a 5% selection), which is a free confirmation that the mod leg is not being double-counted on top of the net path. Months that earn nothing — a six-month term written only in Q4 — report an em-dash and carry zero weight, never a zero, because a 0% loss ratio would drag every average. The Book roll-up is deferred: the walk is per-combo and cross-LOB averaging of monthly loss ratios needs its own weighting argument. |
| D94 | **Phase D was running every time and two at a time, and neither was necessary (user direction, 2026-08-02).** John: *"I don't understand why phase d has to run every single time. Why does it take so long. I have a top end cpu gpu and 64 gb or ram. 45 minutes to generate 7 workbooks is unacceptable."* Generating the seven workbooks takes about ten seconds; the 45 minutes was verification, and he was right about both halves of it. **The concurrency cap was 2, on a 24-core machine with 60 GB.** Measured, a phase-D worker is one Python process plus one Excel instance peaking together at ~670 MB — so six lines were running in three waves for no reason at all. The cap dated from a run where a third line died, which was LATER identified as an access violation rather than memory pressure (D92), and a killed line is now retried serially — so the cap had been quietly absorbing a bug it did not need to absorb, at three times the wall clock. Width is now fitted: `min(lines, cores - 2, two-thirds of free RAM / 900 MB, 8)`, which picks six here and collapses phase D to a single wave. **And phase D now runs only when it can say something.** It mutates inputs, recalculates through Excel and ties the results to the oracle — it tests the ARITHMETIC — so a line that rebuilt to byte-identical formulas and byte-identical defined names since the last green full run cannot produce a different answer, and re-deriving it is ceremony. `release.py` fingerprints every formula and name per workbook (deliberately NOT the file: as-of stamps and cached values change on every build, so the file always differs while the model usually does not), stores the prints from green full runs, and downgrades unchanged lines to phases A-C — printing which and why, because a silent skip is how a verification tier stops existing without anyone deciding to remove it. Any failure clears every stored print, so a later run cannot inherit a green stamp it did not earn; `--force-full` overrides. Measured: 37 min to 26 min on a run where every formula HAD changed, and the common case — a presentation or prose change — now finishes in about two. **The width was then retuned DOWN to four**, because the binding constraint on `--full` turned out not to be memory at all: across three runs at six concurrent Excel instances, with 39 GB free and 24 cores, the harness lost 0, then 1, then 2 lines to access-violation kills. Each kill costs a full serial re-run of that line (~7.5 min), so two of them took a 26-minute run to 47 — more than the sixth worker ever saved. Four gives two waves instead of one and a fan-out that usually completes on the first pass; phases A-C never start Excel and keep the wider cap. The formula is unchanged, only its ceiling: the honest reading is that this fan-out is bounded by COM stability, which no amount of free RAM buys. **The book verification also moved ahead of the fan-out.** Every COM failure this pipeline has produced has been at whichever step followed one, and the book was last: it depends on the lines being built and recalculated, not on their being verified, so there was never a reason for it to run in the exhaust of six parallel Excel instances. Moving it out of that window is a better fix than retrying inside it — the previous run burned 270 seconds failing three times and still went red on a book that passes alone. One diagnosis to correct: I had hypothesised that pywin32's dynamic dispatch was misresolving the zero-argument void `CalculateFullRebuild` as a property and caching its result, which would explain both `AttributeError` and `'bool' object is not callable`. Tested directly, it does not reproduce — the resolution is correct in a clean process. The flakiness is real but is not that, which is why retrying harder never helped. |
| D95 | **The four-slot chronologies were keeping the oldest four, which is exactly the wrong four (user question, 2026-08-02).** John asked what happens past four rate changes on a combo, and then the sharper follow-up: *"Does it show the last 4 or the first 4 on state summary."* It showed the FIRST four. Tested on a combo given six filings, the State Summary displayed 2025-07-01, 2026-03-01, 2026-07-01 and 2027-01-15 — keeping a filing from eighteen months before the plan year that has long since fully earned, and silently dropping BOTH plan-year actions (2027-04-01 +5.0% and 2027-09-01 +4.0%). On a planning exhibit that is backwards: `rl_seq` ranks from oldest, the slots asked for ranks 1..4, and the extra filings a real book accumulates are overwhelmingly NEW ones inside the plan year, so the truncation reliably discards the decisions and keeps the history. The fix is one shared expression, `slot_rank`, used by both four-slot consumers — the State Summary and the `_calc` columns the Book harvests — so they cannot drift: `IF(total > 4, total - 4 + j, j)`. Below the limit the offset is zero and every formula is bit-identical, so a book with four or fewer changes per combo is unaffected. **The eight-slot chronologies deliberately keep first-N**: the Walkthrough builds a CUMULATIVE rate index across its slots (`SUMPRODUCT(...*(rl_seq<=j)*rl_ln1p)`), which only means anything counted from the start, and eight covers essentially any real program. Two things make the truncation honest rather than silent: a Checks advisory naming how many combos are affected (the `# taken` / `# planned` counts beside the slots were already complete, so a row reading "3 taken + 3 planned" against four populated slots was the only existing tell), and a phase-D exercise that appends two plan-year filings so a combo has five and then asserts the slots ARE the last four in order, the oldest is NOT among them, the counts still cover the whole log, and the plan LR still ties a fresh oracle run over all of them. That exercise is the point: the seeded book tops out at three changes per combo, so nothing in the harness had ever reached the truncation branch, which is why a wrong-four bug survived from D61 until someone asked. The MATH was never affected — every cohort's rate index is a SUMPRODUCT over the whole log, and a six-change combo tied the oracle to 1e-9 on all four headline metrics before this change and after it. |
| D96 | **Target loss ratio: an input that no formula reads, on purpose (user direction, 2026-08-02).** John: *"Target loss ratio. (loss ratio needed to make a profit provision). Can we add that in. It's really just a reference column. It's not used in any formulas. Although it may be useful to plot on loss ratio charts."* Added as `lr_target` in tbl_LR, column F — beside the projected LR, its basis and the indication's selected change, because that is the block a reader compares it against. The paste block widens from A:S to A:T. **Reference-only is a design constraint, not an omission**, and it is worth stating because the temptation runs the other way: nothing in the engine, `_calc`, the Solver or Net Delivery reads it, and no Checks row passes or fails on it. Whether a combo "makes" its target depends on expense ratios, reinsurance and mix this workbook deliberately does not carry, so a red/green verdict here would be a claim the model cannot support. What it DOES get is the gap, stated in points and left to the reader: a Bridge banner ("Target loss ratio 61.5% — the plan is 1.86 pts ABOVE it"), two rows in the Bridge's communication metrics, and — the plotting John asked about — the LR Flow chart's dashed reference line, which now defaults to the selected combo's own target instead of needing a value typed on the tab (type one only to plot something else, a stretch target or a book-level benchmark). On the sample that immediately earns its place: the monthly walk opens 4.84 points above target in January and closes 0.65 points below it in December, so the exhibit shows the month the plan crosses its own benchmark, which is exactly the thing a single annual number cannot say. **And on the State Summary, immediately after the plan LR** (user direction, on seeing it): column N, EP-weighted like every other tbl_LR pickup, so a state row shows the answer and the benchmark side by side — `PLAN LR 0.6542 | TARGET 0.6365`. It sits inside the bridge band without joining the product: the four factors still multiply to the plan LR and the mix residual still follows, with the target read against them rather than part of them. That placement forced the reference column all the way through the machinery it was supposed to stay out of — `_calc` publishes `calc_w_target` (target x EP) at column EV, APPENDED after every existing block so no published index moves and `harvest.py` keeps every position it maps; the harvester reads it; the Book's `_book` sheet stores it; and the Book's State Summary shows it, because that exhibit shares `SS_COLS` with the per-LOB one and a shared column list means a shared data requirement. Reference-only still holds where it matters — nothing COMPUTES from it — but 'just a display column' turned out to mean four files, which is worth knowing before the next one. The layout test that pinned the bridge as six contiguous columns failed on the insert, correctly, and now pins the sharper property: the five factor cells are contiguous, target is exactly one past the plan LR, mix is one past target, and the whole comparison still fits the first screen (~1017px). Two mechanical notes. The Bridge's communication block grew by two rows, so the rate-change chronology below it moved from `tot + 9` to `tot + 11` — the kind of silent overlap that would have written a metric on top of a header. And `carry.py` keeps an EXPLICIT column map rather than iterating `LR_COLS`, so a new column has to be added there by hand or a rebuild silently returns it blank; the round-trip test now asserts EVERY seeded tbl_LR column survives, which turns that from a thing to remember into a thing that fails. |
| D97 | **A Dispatch that succeeds can still hand back an object with no type info (2026-08-02).** The release pipeline kept failing at whichever step followed a six-way fan-out, and it presented as four different faults: `AttributeError: Excel.Application.CalculateFullRebuild`, `'bool' object is not callable`, `The interface is unknown`, and an RPC error. Four diagnoses were wrong — a pywin32 property misresolution, a teardown race, a wedged straggler, and "retry harder" — because every one of them was reasoning from `str(e)` on an exception whose traceback `recalc()` was discarding. **Preserving the traceback is the change that actually found it**, and it is the one worth keeping: the frame named `win32com/client/dynamic.py:631 __getattr__`, which is pywin32 building its method map from the type library. That map is built at Dispatch time, so when a `DispatchEx` returns before the type library is readable, the result is a live COM object on which *every* attribute misresolves — hence four symptoms from one cause. Two mitigations: probe `CalculateFullRebuild` / `Quit` / `Workbooks` immediately after `DispatchEx` and re-dispatch if any is missing (6 attempts, backing off to ~42s), and **stagger Excel launches 2s apart** so instances never initialise simultaneously, which removes the contention rather than retrying through it. | Honest about what this did NOT fix: the probe alone was not enough — Property still failed under 6-way concurrency after it landed, which is what forced the stagger. And the stagger has not eliminated the failure either: the run that produced this entry still lost one phase-D exercise (`ex34`, Umbrella) to "No recalculation engine available". The concurrency cap came down 6 -> 4 for the same reason, because a killed line costs a ~7.5-minute serial retry and one run reached 47 minutes that way. So this is a reduction, not a cure, and the reason to record it is that the next COM failure will present as a *fifth* unrelated-looking error. Read the traceback, not the message. **Corrected the same night by D100**: it did present as a fifth error, and this entry's diagnosis did not cover it. The missing-type-info fault was real and the probe still earns its place, but the failures that followed were `RPC_E_CALL_REJECTED` — a different cause needing the opposite response. Treat the paragraph above as one COM failure mode, not as the explanation of COM failures. |
| D98 | **Keying a companion note by list position is a bug waiting for the next insertion (2026-08-02).** The Bridge's resolver block writes four advisory notes in column D — the M_prior and M_2 sentinel explanations, the A_other-needs-a-label warning, and the net-selection banner — and they were keyed by *index into the resolver input list*. Adding the target loss ratio (D96) at index 12 slid every later note one row down from the label it describes. The A_other warning printed beside "Net trend", and the net-selection note landed on "Mod adjustment effective" — whose cell holds the **text** "ON"/"OFF", making `IF(<text>,...)` a permanent `#VALUE!`. Notes are now keyed by the row's defined NAME (`nr_MPrior`, `nr_M2`, `nr_AOther`, `nr_NetMode`), which cannot drift when a row is inserted; a missing name raises `KeyError` at build time instead of silently misplacing a note. `tests/test_bridge.py` builds a workbook and pins each note to its own row, from both directions — every named row has its note, and no other row has one. | The part worth sitting with is how far it got. `Bridge!D55=#VALUE!` shipped in all six workbooks, and **the workbook's own Checks panel still reported PASS**, because no check watches an advisory cell. The harness did catch it — phase B's error scan fails on it and exits 1 — but it surfaced only as sixteen phase-D failures in a truncated release log that reported exit 0, so the signal arrived late and looked like something else. Two lessons, kept separately: an exhibit's *decorative* cells are still formulas and still need an invariant, and the release driver's reporting is now itself a thing to verify. The same insertion also consumed the resolver block's last spare row (it now ends at 58, with the waterfall's chart staging at 59), so `build_bridge` raises if the block would reach `BR_CHART_DATA` — growing into the chart data would have overwritten the waterfall's own series with no visible failure at all. |
| D99 | **Reverting D91: chart axes go back to Excel's automatic scale (user direction, 2026-08-02).** John, on seeing the framed windows in the shipped v3.4.3 workbooks: *"let's revert to the auto axes on the charts. the fixed option is even worse. i'll just have to adjust them. not a huge deal."* Removed: `zoom_axis`/`axis_window` from `xlstyle`, `chart_axis_windows` from the build (with it, one oracle run per combo at build time), the five call sites on the Bridge, One-Pager, LR Flow and the three Flow Dashboard charts, the eight baked `axw_*` constants on `_oracle`, the four `fd_*` named series that existed only to be checked against them, and the two Checks advisories that watched for a paste leaving the window. | D91's diagnosis was right and its remedy was wrong, which is the more useful half of this entry. Framing a levels chart on its data IS better than a zero floor — but the window has to be baked, because an Excel axis bound is a static number and not a formula, and a bound that cannot follow the data it frames turns every paste into a potential off-the-edge chart. That is why D91 needed two Checks rows and a `--carry-forward` regeneration story to be safe at all; a presentation choice that arrives with its own failure mode and its own maintenance ritual was buying less than it cost. The reader adjusts two or three charts by hand instead, which no code has to keep true. The two retired Checks rows are DELETED rather than pinned permanently green: append-only protects a reader's row references while a check still means something, and a row asserting the book has not left a window that no longer exists means nothing. |
| D100 | **"Call was rejected by callee" means BUSY, and the retry was treating it as BROKEN (2026-08-03).** Phase D failed on every line, at 4, 2 and 1 parallel workers alike, always at a different exercise (`ex04`, `ex05`, `ex06`, `ex08`, `ex20`, `ex21`). Running one line directly and reading the preserved traceback gave the actual fault: `pywintypes.com_error: (-2147418111, 'Call was rejected by callee.')` — `RPC_E_CALL_REJECTED`, raised from `xl.CalculateFullRebuild()`. The Dispatch had succeeded and the member had resolved; Excel simply had not finished the previous request. `recalc_with_excel` caught it in a blanket `except Exception` whose recovery was `shutdown_excel()` — so a healthy-but-busy instance was destroyed and replaced. **That is the one response that makes a busy signal worse**: Excel's teardown outlasts its `Quit()` (D92), so the replacement initialises while its predecessor is still dying, which is exactly when the next call is rejected too. The retry was feeding the failure it existed to absorb, and it did so identically at every concurrency, which is why "reduce the worker count" never fixed it. Added `busy_retry()`, which re-issues a single COM call — up to 120 times, 0.25s apart, pumping the message queue between tries — and re-raises anything that is not a busy HRESULT. Applied to every call that can be rejected: `Workbooks.Open`, the cell writes, `CalculateFullRebuild`, the `CalculationState` poll, `Save`, `Close`, and the type-info probe itself. Property went from failing at `ex06` to **314 passed / 0 failed** on the same artifact. | Two things worth keeping. First, the diagnostic method rather than the fix: four wrong theories (D97) and three lost runs came from reading `str(e)` and from the release driver's filtered tail, which reported the *last* line containing "Error" — invariably `LibreOffice (soffice) not found`, the fallback engine's complaint, naming the one engine that was never going to run on this machine. The real error was two frames up the whole time. Running the failing line ALONE, unfiltered, took four minutes and ended it. Second, the honest cost: absorbing rejections is slower, about 37s per exercise against a nominal 14s, because the waiting that used to present as a crash is now visible as time. That is the correct trade — a verification tier that finishes in 22 minutes is worth more than one that fails in 7 — but it means phase D's runtime now reports how contended the machine is, and a slow run is information, not a regression. |
| D101 | **The State Summary is an exhibit, not a dashboard: inputs, then the answer (user direction, 2026-08-03).** John: *"in my mind it is the logical flow, here are all the inputs, here is the output. being able to collapse them if you don't care and just want the answer would be nice."* The rate-change chronology, the schedule mods and the engine levels now sit to the LEFT of the bridge, in the order the engine consumes them — what we filed, what we assumed, what that earns, therefore this loss ratio. The bridge chain is itself a left-to-right product, so its inputs belong on the same axis rather than behind it. Two NESTED column outline levels make the width affordable: level 2 is the chronology alone (12 columns, shipped collapsed), level 1 is the whole input region, so one more click leaves `State | EP | Plan LR`. Nesting is what buys two independent toggles — contiguous columns at a single level merge into one group. Also fixed here: **CY P and CY P+1 were 20 columns apart**, separated by the chronology and the counts for no reason other than the order the blocks were built in. They are the same quantity for two consecutive years and now sit six columns apart, both on one screen. Declared on `SsCol.level` beside the column, and applied by one `ss_outline()` shared with the Book's mirror so the two cannot drift into different collapse behaviour. | **This reverses D90, and D90 was not wrong about the problem — it was wrong about the cure.** Plan LR really was stranded at ~1,600px, off the right edge of a 1080p window, and a flagship exhibit that cannot show its own headline is broken. But reordering bought that by breaking the argument's order, and it was never the only fix available: collapsing the inputs reaches the same headline while leaving the derivation intact. Measured on the sample book, Plan LR sits at **949px as shipped** — identical to what D90 achieved — and at **537px with the inputs collapsed**, which is better than D90 managed. So the new layout is not a trade against the old one; it dominates it, and the reason the trade looked necessary was that nobody had tried the third option. The test that pinned D90 asserted raw column width to Plan LR, which no longer describes anything real; it now asserts the property in each collapse state, including that the fully expanded exhibit IS wide (>1400px) — because if it were not, the toggle would be pointless. |
| D102 | **Net Delivery shows the two years you can set a target for, not the year behind you (user direction, 2026-08-03).** John: *"I don't see any reason to have p-1. This tab is used for future decisions. Here's a target and how to get to it."* The grids showed P−1 beside P (D68). But a net target is entered at 1/1 of the plan year and optionally 1/1 of P+1 — so the display opened on a year with no target in it, and the required-pricing grid could not populate that half at all: twelve columns per state whose only content was a caption, `"no target in P−1 — nothing to solve"`. A layout that needs a note explaining why part of it is blank is telling you something. Now navy = P, steel = P+1, on BOTH grids. **P+1 is carryover only**: no second filing is assumed, because nobody plans a rate change two years out (John, on being asked). So it answers what THIS year's decision leaves behind — the rate the plan-year program still delivers as its anniversaries roll through, and the pricing gap that remains against the P+1 target. New oracle `net_delivery_by_month_p1`; `_netcalc` gains `AA` (the P+1 target, using the engine's own `net_x1` fallback) and writes `AG`/`AH`/`AI` for the P+1 cohort rows. | The arithmetic needed no new machinery, which is the tell that the year pair was the only thing wrong: every P+1 month lies after D, so `W_new = W x (1 + r)` with no blend, and its year-ago denominator is the plan-year month — exactly the affine `a + b*r` the plan-year decomposition already carries. The workbook reads the plan rows rather than recomputing them, so the two halves cannot drift. **What I got wrong first**: I told John this was "a display-layer change, not an engine one." It was, for the rate leg; it was not for the pricing leg, where `AH` divides by `E{t}` — the P target — on every row. Pointing the grid at the P+1 offsets without wiring the second target would have solved every P+1 month against the wrong target, quietly, in the exhibit the filing decision comes from. Reading the formula before promising the estimate is the whole lesson. On the sample book the exhibit immediately earns itself: KY's rate program is exhausted by April 2028, so the full +10% target sits on schedule mod for nine consecutive months — which the P−1 block could never have said. The prior-year cells stay COMPUTED in `_netcalc` though no longer displayed: the harness ties them to `program_flow_by_month`, a cheap independent check that the cohort offsets have not slipped, which is exactly the failure D64 hit silently. |
| D103 | **The book as one long table, so a single calculated field cannot be wrong (user direction, 2026-08-03).** John: *"how cool would it be if we could add a pivot table friendly dataset (premium weighted) of all the line workbooks... then we could filter on any combination we wanted"*, and, asked wide or long: *"I generally prefer long datasets. Wide have their place but not here."* New visible **Pivot Data** sheet on the book: `LOB | BU | State | Category | Measure | Month | Weight | Weighted value`, one row per combo per measure and per month for the three monthly legs — 20,341 rows as an Excel Table (`tbl_Pivot`, so a pivot built on it grows with the roster). 18 annual measures plus delivered / rate / mod by month. The reader adds ONE calculated field, `= 'Weighted value' / Weight`, and it is the correct premium-weighted answer for every measure, every slice, every subtotal. **No pivot table is shipped**: openpyxl can round-trip an existing pivot but not create one, and calculated fields it cannot do at all; building it through Excel COM was possible and rejected as machinery to maintain for something the reader assembles in thirty seconds. | Long is not a taste here, it is the safety property. The three monthly legs **do not share a denominator** — rate and delivered weight by `epw`, the mod leg by `epwm`, which is additionally gated by the combo's mod-adjustment flag so combos with it off drop out instead of entering as zeros. Wide, nothing stops a reader pairing the wrong numerator and denominator and getting a plausible wrong number. Long, each row carries its own weight and one formula is right everywhere. Three consequences worth stating: **(1)** there is deliberately no raw `Value` column — the calculated field is exact even at n=1, so a value column would add nothing except the one field that could be dragged in and averaged, and averaging loss ratios across states is precisely the mistake this table is shaped to prevent; **(2)** rows with no weight are OMITTED, not written as zeros — a combo with no premium and a month whose mod adjustment is off have no answer rather than an answer of zero, and an absent row cannot drag an average; **(3)** the monthly mass is rescaled by 1/12 so `Weight` means PREMIUM in every row — raw `epw` sums to 12xEP because the seasonality weight averages 1, which is a fine relative mass and a confusing thing to sit beside annual rows carrying EP itself. Rescaled, a month's weight is that month's written premium and the twelve sum back to EP, which the harness checks. **The limit John found by asking**: monthly premium comes from `tbl_Seasonality`, per state, and a blank row means UNIFORM. Unless that table is populated, month-to-month shape in a pivot is coming from rate and mod anniversaries, not from volume — and the seasonality is state-keyed, so two lines writing the same state on different rhythms are not distinguished. The Read Me says so where someone slicing by month will read it. |
| D104 | **The Book's State Summary had no conditional formatting, for four versions (user catch, 2026-08-03).** John, after the D96 target column and the D101 reorder both moved the rate-change slots: *"make sure conditional formatting for the t* and p* for taken and planned on state summary didn't get shifted."* The per-LOB rules had NOT shifted — they address columns through `ss_l()`, so the token rules travelled from P..AA to E..N with their columns, and the formula anchors still matched their ranges. But checking the same thing on the Book turned up that its mirror carried **no conditional formatting at all**: no plan-LR heatmap, no EP data bar, and no amber on planned rate changes, so a reader could not tell a T from a P at a glance on the one exhibit that shows every line at once. Extracted `ss_conditional_formats(ws, first, last, font_name)` in `sheets_main` and called it from both builders, alongside the `ss_outline` it already shares. Also replaced the last hard-coded literal in that block — the EP data bar was pinned to `B`, correct only by luck after the reorder — with `ss_l("ep")`. | The interesting part is WHY it drifted for so long. The two builders had been unified on everything a test could see: the column map (D90), the group captions, the outline levels (D101). Formatting was the last thing still hand-written in one place only, so it was the last thing free to diverge — and because the Book's version was *absent* rather than *wrong*, nothing looked broken. An omission is the hardest kind of drift to notice: a misplaced rule shows up as colour in the wrong column, a missing one just looks plain. Two tests now pin what the shared helper PRODUCES rather than that it was called — every rule lands on the column the map names, no rule lands anywhere else (a stray one means a literal letter crept back), and every token formula anchors on its own range's first row, because an anchor that disagrees with its range makes each row test its neighbour's cell. That second test is the one that would have caught a genuine shift, which is what John actually asked about. |
| D106 | **A rate change that never happened, shown as 1/0/00 (user catch, first real use, 2026-08-05).** John, after using the book for the first time: *"in the summary book, state summary tab, rate change history, when there is no rate change, it shows 1/0/00 and 0% instead of blank like in the line workbooks."* The per-LOB exhibit was right and the book was wrong, and the difference is a **round trip through a value**. `_calc` publishes an unfiled slot as `""` and the per-LOB State Summary reads that straight into its own formula, so the empty string never stops being one. The book cannot: the harvester reads cached values with openpyxl, which reports a cached `""` as `None`, `build_book_data` writes `None` as a genuinely blank cell, and **`INDEX` over a blank cell returns numeric 0, not ""**. Under `m/d/yy` that renders `1/0/00` (serial 0 is "Jan 0, 1900"); under the signed percent it takes the zero section and renders `0.0%`; in the General-formatted token column it renders `0`. Fixed in the book's formula with a second guard — presence is tested on the slot's DATE via `COUNT(INDEX(...))`, which is 0 for a blank and 1 for any real serial, and all three parts of a slot follow it. Shipped with the book's TOTAL row finally shading its chronology band (the per-LOB total blanks those twelve columns explicitly; the book's stopped twelve columns short) and `pivot_rows` omitting a `None` measure instead of `or 0.0`-ing it into a real zero at full premium weight. | Three things worth keeping. **(1) The guard that existed was the wrong guard.** `IF(cnt<>1,"—",…)` protects the multi-combo case — is this row ONE combo? — and reads as though it protects emptiness too. It does not, and the two questions are independent: a single combo with no rate changes passes the first test and fails on the second. **(2) It was invisible in the default view.** Every slot cell shows `"—"` until the filters resolve to one combo, so the shipped book's cached values were all dashes and four versions of looking at it proved nothing. It took someone actually filtering to a line and a BU — i.e. first real use — for a single cell to evaluate down the broken branch. The regression check added to `verify_book` phase D is deliberately the single-combo exercise, and it asserts the biconditional both ways: a slot the combo filed must NOT be blank, and one it did not file must be. **(3) The obvious cheaper fixes are worse.** Coercing `None → ""` at harvest looks smaller, but openpyxl writes a `""` cell as an `inlineStr` with no child and reads it back as `None`, so the coercion does not survive its own round trip — and this codebase has already lost a day to Excel refusing a workbook over exactly that class of thing (D41). Number formats can hide the zero (`m/d/yy;m/d/yy;;@`) but leave the cell's VALUE at 0 for anything that copies, charts or reads it, and do nothing at all for the General-formatted token. And guarding at publish would break the per-LOB exhibit, which is not broken. |  John: *"can you independently verify your phase d testing is the right thing to do... seems ridiculous that it takes 45 minutes to do 50 iterations on 6 files."* It was ridiculous, and the profile said why: **3.49s per exercise, of which the recalculation was 0.02s**. The other 99% was ceremony — copy the 2.7MB file, reopen it in openpyxl, write the mutation, save, hand it to Excel, let Excel open and save it again, parse the result back in openpyxl twice. New `tools/live.py` opens ONE workbook in Excel and keeps it: an exercise writes its cells, calls `Calculate`, reads the answers out of the live grid, and puts the cells back. `Application.Calculate` suffices after one `CalculateFullRebuild` on open because this family bans every volatile and indirect function, so the dependency graph is static and a changed input dirties exactly its dependants — which is why plain Calculate is 17x cheaper than a rebuild. Reads are BULK: a single-cell COM read costs ~3.5ms, so phase D's 63-cell state loops would have cost more than the arithmetic they check, while one `UsedRange.Value` for a whole sheet costs 0.03–0.18s and serves every cell on it. The shims present the openpyxl surface the assertions already use, so ~1,000 lines of them are untouched. **Property: 568.4s → 17.8s, and all 317 assertions identical — same descriptions, same order, same verdicts.** Six lines now verify in ~2 minutes, serially. | **This is D86a reversed, and D86a was right at the time.** Writing mutations through COM was tried and reverted because under six concurrent Excel instances a write could FAIL SILENTLY: one line returned a full set of assertion failures whose "actual" values were the DEFAULT workbook. A harness that can quietly fail to set up its own exercise can just as quietly report a false PASS — which is the only bug class that matters in a verifier. So the return to COM writes had to pay for itself three ways. **(1)** Every mutated cell is re-read after calculating and a mismatch RAISES. **(2)** A canary set of headline values is re-read after every restore, catching the opposite leak — a restore that did not land, contaminating every later exercise. **(3)** It is fast enough to run SERIALLY, so `HARD_JOB_CAP_FULL` drops 4 → 1 and the concurrency that caused the original instability is simply gone (D100: those errors were RPC_E_CALL_REJECTED — *busy* — from instances contending, not broken workbooks). Both guards are tested by breaking them on purpose. Three COM behaviours were measured before any of it was written, not assumed: an error cell marshals as an **int** with Excel's code in the low 16 bits while every real number arrives as a float, so the type alone identifies an error; `.Value` returns dates as datetimes but stamped with the machine's timezone, which must be stripped or the date can shift a day east of UTC; and a formula returning "" reads as `""` through COM but `None` through openpyxl. A differential test then compared **1,197,707 cells** of a real workbook, shim against openpyxl: zero differences, 294 defined names identical. The old path stays behind `--legacy-phase-d` as the second opinion. |
| D107 | **The Inputs sheet is four blocks now, and two columns fewer despite eight new ones (user direction, 2026-08-05).** John, after his first real use: *"i need additional inputs on the inputs tab and maybe a reordering... all of the additional factors are just carry through, they won't affect calcualtions in the workbook, they are just there for completeness... i think all the indication inputs should be together, then all the mods. we can likely remove the lr basis and the accompanying indication selected (chg) column. i am not sure we would ever use this functionality."* `tbl_LR` goes 20 columns to 25, A:T to A:Y, in four blocks: **who the row is and how big it is** (BU, State, and adjusted plan EP, moved forward from column N because it is the first thing anyone looks up and the weight behind every aggregate); **the indication** (projected LR, prospective premium and loss trends, expense ratio, ALAE, ULAE, combined ratio, target LR, cat load, large-loss load); **the mods**, chronological as before; and the **plan-year levers**. Eight of the nine new fields are pure carry-through, extending the D96 contract — nothing in the engine, `_calc`, the Solver or Net Delivery reads them, and no Checks row passes or fails on them. Removed: **LR basis** and its **selected change (s)**, and **Reason for other adj**. `nr_LRcur` KEEPS its name and its place in the bridge, now `=nr_LRproj`: the concept — the LR the bridge starts from — did not change, only the arithmetic that produced it, and a dozen exhibits already read that name. | **The ninth field was already there, and it is the one that pays for the other eight.** Target LR (D96) relates to four of the newcomers arithmetically — target = (combined − expense) / (ALAE × ULAE) — so the block can now check itself. It does, as an **ADVISORY**: which loads a line puts inside the combined ratio and which it carries separately is a convention this workbook does not get to overrule, so a mismatch is disclosed and never fails the panel. Two details in that formula are deliberate. It is **multiplied through** rather than divided, because dividing by a blank ALAE is a `#DIV/0!` on a row nobody has filled in yet — and the tolerance rides along with the multiplier so it stays 0.5 points on the target. And the sample data DERIVES its combined ratio from its target, so the identity holds exactly on every seeded row and one combo is seeded 2 points off ON PURPOSE — a check nobody has seen fire is a check nobody knows works. **Removing an input is not the cheap half.** The basis reached further than a column: an engine field (`ComboInputs.lr_basis`, a required positional argument), a validation list, a Bridge resolver row, a waterfall STEP with its own chart category, a Portfolio column, a Control display toggle (D16, now superseded), a `_oracle` fingerprint row, two Checks rows, a phase-D exercise and a selector pick. Two consequences worth stating. **(1)** The waterfall chart's category spans were hard-coded row offsets; with the basis step gone every one shifted, so they and the chart's data range are now sized off `len(cats)` — the same lesson as the Walkthrough hyperlinks in D106, one file over. **(2) Carry-forward can now CHANGE A VALUE, and says so.** A pre-v3.8 book with a row marked `proposed` holds a loss ratio at proposed rate level; v3.8 defines the input to be at current level, so `carry.py` folds those rows by ×(1 + s) on the way in and prints what it did. `CarriedInputs` grew a `notes` field for exactly this: a rebuild that alters a number owes the user a sentence. The knock-on is that a pristine v3.7 SAMPLE file no longer fingerprints as the sample once folded, so the D82 overwrite guard refuses instead of overwriting — which errs in the safe direction, and is why regenerating the shipped samples took `--force` once. |
| D108 | **A Checks row that tested one row and reported on sixty-three (found by writing another one, 2026-08-05).** The D107 target-LR advisory was written, shipped green, and did not fire on a combo seeded 1.85 points off ON PURPOSE. The cause is a property of classic Excel worth writing down: **`N()` does not broadcast over a range inside SUMPRODUCT — it collapses to the range's FIRST cell.** So `SUMPRODUCT((mask)*(N(range)>x)*1)` is not "count the rows where range > x"; it is "if the FIRST cell is > x, return the number of masked rows, else 0". Probed against a live workbook: with a violation planted in tbl_LR row 1 the formula returned **63** (the whole populated row count), and with the same violation in row 8 it returned **0**. The new advisory was rewritten with `ISNUMBER`, which does broadcast — and so were the **two rows that already had the bug**: "M_0 as-of dates precede 12/31 of the plan year" (a **FAIL**-severity input guard, inert for every row but the first since v3.3) and "Populated seasonality rows have a positive weight sum" (WARN). Both now catch a violation in any row and report its true count. | **The reason this hid so long is that it fails toward PASS.** A row wired this way returns 0 on healthy data, which is exactly what a working check returns — there is no visible difference between "nothing is wrong" and "I am not looking". It only surfaced because a new check was written whose failure case had been *deliberately seeded*, and the check stayed silent: the same reason D106's regression test reverts the fix to confirm 186 violations come back. A check nobody has watched fail is a claim, not a check. Two things follow. **(1)** `tests/test_checks_formulas.py` now fails the build on any Checks formula applying `N()` to a **multi-cell** defined name — the general form rather than the three instances, resolved against the built workbook so it knows which names span more than one cell. Single-cell `N()` is untouched; that is what the function is for. **(2)** The paste-hygiene `ISTEXT` rows had always worked, and that was the clue: IS-functions broadcast, `N()` does not, and the codebase had been quietly relying on both behaving the same way. Where the fixed rows now do arithmetic on a column (`lr_target*lr_alae*...`), a text paste makes the row `#VALUE!` rather than silently wrong — loud, and already a FAIL on the hygiene row that names the offending column. |
| D109 | **A workbook for the reader rather than the analyst — six fewer tabs, same arithmetic (user direction, 2026-08-05).** John: *"to avoid the workbook being too cumbersome, can we have an option to create a 'light' workbook... maybe an excel control file that allows you to select tabs you want. lets discuss options."* Two of his three ideas turned out to be impossible or pointless and the third was right. **An Excel control cell cannot hide tabs**: sheet visibility is not a formula-reachable property in .xlsx, only VBA or a manual right-click changes it, and macros would forfeit the no-VBA guarantee the whole verification rests on. **Hiding rather than removing saves nothing**: a hidden sheet is fully present, fully calculated and fully in the file. So the answer is a per-LOB `profile: light` in config that genuinely does not BUILD **Portfolio, Scenarios, Solver, Attribution, Compare and One-Pager** — nor the three hidden `_calc` sections that exist only to serve them. Measured on Property: 26 sheets to 20, **229,650 formulas to 200,471 (-12.7%)**, 3.02MB to 2.64MB. It recalculates with **zero formula errors** and verifies through phase D at **285 checks / 0 failed**. | **The tab list is not a matter of taste, and the dependency map decided most of it.** Bridge is not on it despite a light reader never opening it: Bridge hosts 21 `nr_*` names that ten surviving sheets read, and the Rate and Mod Logs host the `rl_*`/`ml_*` families the engines are built from. What CAN go are the leaves — plus four couplings that had to be cut by hand: Control's scenario-KPI block, Net Delivery's delta-net note, `_netcalc`'s bound-warning column (the three reasonability bounds are editable cells ON the Solver sheet, so they fall back to the same numbers baked as literals), and six Checks rows. **The structural risk was `_calc`.** Its sections were chained — each derived its anchor row from the end of the one before — so skipping any of them left the rest pointing into space. They run off a **cursor** now, which a skipped section simply does not advance. That refactor was done by script and then PROVED inert: a full build before and after was compared cell by cell, all **244,155 populated cells and 293 defined names identical**. **The harness needed one idea, not five.** A phase-D exercise already declares the sheets it depends on — its mutation dict names them — so one guard in `run()` skips any exercise that types into a tab this file lacks, and prints which. Exactly one exercise needed more (it types into the Rate Log and then reads the Solver's answer), which is what the `needs=` argument is for. The guards key on **the sheets in the file, not the config**: the harness verifies the workbook in front of it. And the failure mode is self-announcing — a skipped builder registers no defined name, so anything still pointing at one is `#NAME?`, which phase B catches. `tests/test_light_profile.py` does the cheap half in seconds without Excel: no surviving formula may mention a dropped tab, and no defined name may point at an absent sheet. |
| D110 | **Net Delivery reaches the book by REPORTING, because it cannot get there by solving (user direction, 2026-08-05).** John: *"if feasible well need net delivery brought into the summary workbook"*, and, told what full parity would cost: *"lite, but i was hoping for the flow exhibits just as they are in the workbook as well, only premium weighted. if this is more of the full parity flavor, do not do it."* The per-LOB Net Delivery tab cannot be harvested and the reason is structural: its `_netcalc` blocks are keyed `nd_BU & "|" & state` — **one business unit at a time, chosen by a picker** — and its answers depend on dates and filed percentages typed on the sheet. There is no per-combo result to publish without building a second engine of ~63 blocks per workbook, which is what John declined. So the book gets the half that IS harvestable: **the two 24-month grids, premium-weighted**, plus the targets and the gap. The line workbook SOLVES (what should I file?); the book REPORTS (what does the program as logged deliver against what was asserted?). | **The wave collapsed from "double `_netcalc`" to "publish 26 columns" on one observation**: the cohort blocks already span Jan P-2..Dec P+1, and `program_flow_by_month` already returns 24 months. The following year's legs are the SAME formula one year forward — `t+39+j` over `t+27+j` — publishing rows that were always computed and never read, exactly the trick D68 used one year back. No new engine section, no recalc cost, and no `epw` twin: the seasonality weight is a function of calendar MONTH alone, so one weight family serves all three years. **Verified before anything was built on top**: all 1,512 published P+1 legs across 63 combos tie a fresh oracle run, and a harness check asserts the following year is not merely a COPY of the plan year — an off-by-twelve here would have been entirely plausible to look at. **Three honesty rules the exhibit keeps.** (1) The required pricing leg `(1+x)/(1+rate)-1` does **not** aggregate — a ratio of weighted means is not the weighted mean of ratios — so it appears only where the filters leave exactly one net-mode combo and is dashed everywhere else rather than approximated. (2) Targets average over the combos that ASSERT one; a combo asserting nothing is not a target of zero, so a state with none shows its delivery and a dash. Delivered, by contrast, is premium-weighted across every combo in view, which is the book statistic. (3) The gap is the distance between what has been filed and planned and what was asserted — the per-line Solver's SUGGESTED filings are deliberately not injected, so a gap means work outstanding rather than an error, and the sheet says so. The P+1 target carries the workbook's own blank-carries-forward fallback plus a flag recording whether anyone actually chose it, which the summary reports as entered / carried / mixed. |
| D111 | **The carry-through indication fields, premium-weighted — and the weight is the interesting half (user direction, 2026-08-06).** John: *"i would like the additional variables i added premium weighted and added to the pivot table in the summary workbook... they dont really flow into any calculations but i would like them there to see weighted averages if we wanted to."* The eight D107 fields — prospective premium and loss trend, expense ratio, ALAE, ULAE, combined ratio, cat load, large-loss load — are now published `x EP` on `_calc`, harvested, and appear as an **Indication** category on the Book's Pivot Data, where one calculated field gives the premium-weighted answer for any slice. Still reference only: nothing computes from them. | **The obvious implementation is wrong, and it is wrong in the direction that looks right.** Every other weighted metric here publishes `N(x) x EP`, but `N()` of a BLANK cell is zero — and all eight of these are OPTIONAL. A line that has not filled in an expense ratio would enter the book average as an expense ratio of 0% carrying its full premium, dragging the mean down in a way that reads as a real number rather than a missing one. So each field publishes **two** columns: the weighted value, and `ep_x` — the premium of the rows that actually carry a value. `SUM(w_x)/SUM(ep_x)` then averages over the book that answered. `PIVOT_ANNUAL` grew a per-measure weight field for exactly this, `None` meaning "every combo has one, weight by EP". A consequence worth stating on the Read Me, and stated there: a slice's WEIGHT on these measures can be less than its EP, and that difference is the premium with no answer. The same treatment was applied to **Target LR**, which has carried this flaw since D96 and never shown it — every sample row has a target, so the only way to see it was to write the next measure with the same shape and think about it. The populated test is `COUNT(INDEX(range,n))=0` rather than `=""`: INDEX over a blank cell returns 0, not "", so `=""` would call every blank populated — the same Excel fact that made D106's `1/0/00`. |
| D112 | **The earning parallelogram, made visible — and every name explains itself (user direction, 2026-08-07).** John, opening the simplification conversation: *"one thing i think could be really powerful is some type of graph that is shaded by earned level for the current year and plan year."* The data had existed since v1: the Rate Engine's e(k, m) matrix is 48 cohorts x 36 months of earned shares — it just rendered as 1,728 grey `0.000`s. Four changes, all presentation on top of already-verified cells: **(1)** the matrix is now a **heatmap** — one colour scale (white -> steel) over `V17:BE64`, zeros blanked by the number format's third section (`"0.000;;"`), headers year-banded navy = plan year / steel = neighbours — so the shaded diagonal band IS the classic actuarial earning diagram, live for the selected combo; **(2)** an **area chart** above it, Jan (P-1) .. Dec P: shaded area = earned level `E_m`, navy line = written in-force level, the vertical gap = the unearned runway, staged in 24 outline-hidden rows below the strip like every other chart block; **(3)** a Net Delivery band row — **"Share earned within plan yr — ec(k,P)"**, column Q of the cohort block — free context for the delivery conversation (a December filing rates a twenty-fourth of a year); **(4)** every single-cell defined name on a visible sheet now carries its Methodology description as a **hover note** — 107 of them, so `=nr_Arate_P` explains itself where the reader already is instead of on a tab they must know to visit. | **Shading that cannot lie, notes that cannot drift.** Conditional formatting is evaluated at render time and cannot change a value, so the heatmap adds zero calculation surface — and the cells it shades were formulas all along, so the harness now ties a cross-section of the matrix itself to the oracle's `e(k, m)`: both half-month edges (offset 0 and t), an interior month, and the first out-of-window zero, in a prior-year and a plan-year month, which keeps working when a 6-month line makes the band twice as steep. The zero-blanking is deliberately a NUMBER FORMAT, not a formula change: anything that reads, charts or copies the cell still gets the real 0 — D106's lesson runs both directions: hide at render, never at value, when the value is genuine. The notes are emitted from `ctx.names` — the same registry `flush_names` writes and the harness binds by — so the hover text is generated from the tested artifact rather than maintained beside it; the walkthrough-drift rule (D-series constant: docs are emitted, never hand-kept) now covers cell-level documentation too. A phase-A check asserts the notes SURVIVE Excel's resave, which they do because openpyxl writes legacy notes — the durable kind — not threaded comments. Line verification: 318 -> 320 checks (12-month), 316 -> 318 (Inland Marine). |
| D113 | **One arithmetic, two dialects — LET arrives, gated on its own spike (user direction, 2026-08-07).** John, reviewing the simplification research: *"Can you compare the two and tell me which version you think is better."* First the gate the plan demanded, because openpyxl LET authoring was folklore and LAMBDA-in-a-defined-name pure inference: the S0 spike wrote `_xlfn.LET(_xlpm...)` formulas and LAMBDA names with openpyxl, opened them in real Excel, computed, resaved, re-read — **37/37, both features**. Then `formula_mode: modern` became real (the config hook D18 validated-but-refused now has a second honest value). The mechanism is `xlstyle.let_`: each converted formula is ONE template — `[(name, expr), ...]` pairs plus a body referencing `{name}` tokens — from which CLASSIC expands every variable inline (the exact repeated-inline shape these formulas always had; for every all-function-call template the output is byte-identical to the hand-written original) and MODERN emits `_xlfn.LET` with `_xlpm.` stamped on every variable occurrence, the S0 lesson made unforgettable by a build-time raise on any unresolved token. Six families converted — the State Summary slot resolver (the 420-char, triple-COUNTIFS flagship), Rate Log share-of-year and same-row lookup, Flow Dashboard carryover shares, Mod Engine's M_endPrior fallback, Net Delivery's state target, LR Flow's race sentence — **1,130 LET formulas** in the pilot build. The proof is the pilot pair: Property built BOTH ways from identical carried inputs, both recalculated by Excel, and **223,668 populated cells across all 24 comparable sheets came back IDENTICAL** (`tools/ab_compare.py`; Read Me and Checks excluded by design — they carry the dialect surface). The modern file verifies **321 / 0 through phase D**. It ships beside the fleet as `_MODERN` for the user test that decides adoption; classic remains the default everywhere. | **The un-ban is narrow on purpose, and the sentinel is why it is safe to show people.** MODERN_OK = {LET} and nothing else: the volatile/indirect functions stay banned in both dialects because `Application.Calculate` sufficiency (D105's whole speed model) rests on the static dependency graph; the error-swallowers stay banned because the zero-error sweep is only meaningful while an error cannot hide; the spill family stays banned because openpyxl cannot author `cm` metadata and every tie, harvest position and paste block is address-pinned. The harness reads the dialect FROM THE FILE (the canary name exists only in a modern build — the D109 principle again), and a new phase-A check polices storage-prefix discipline: classic tolerates no `_xlfn.` at all — which BANNED_RE never actually caught, its lookbehind excludes the dot — and modern tolerates exactly `_xlfn.LET`. The degradation story: a modern file on pre-2021 Excel would silently show the last-saved values, so the Read Me carries a CLASSIC-dialect banner (`IF(ISERROR(nr_ModernCanary),...)`) plus a FAIL Checks row, and `fullCalcOnLoad` (D88) is what trips them on open — the same setting that once guarded stale-calc sessions now guards a whole dialect. What LET buys beyond reading: the D95 bug class — N inlined copies of one subexpression edited in lockstep — cannot recur in EITHER dialect, because there is one template and the copies are machine-expanded; State Summary alone drops **3,145 -> 2,089** COUNTIFS/SUMIFS evaluations (-34%), and the converted cells shed ~44k chars of display text. Two scope rules keep D18's "double audit surface" objection answered: conversion touches PRESENTATION formulas only (`_calc` and the engine blocks stay decomposed cells — they are the tie surface), and the A/B run makes the dialects prove each other on every release of the pilot. One deliberate exception to single-source: Mod Engine C12's classic branch still calls the shared `mod_drift_at_switch` verbatim (other emitters use it), so its modern LET is a site-local template and the A/B value tie is the sync proof there. Verification counts move +1 everywhere (the prefix-discipline check): lines 321 / IM 319. |
